# Algo-Trading-Projekt — Planungsdokument

## Schicht 1 — Übergeordnetes Ziel

**Das ist das Ziel von allem hier** — Wiki, `raw/marktdaten/`, `tools/analyze_ohlc.py`, dieser
Ordner: ein Handelsalgorithmus, der **selbststaendig und allein ueber Interactive Brokers**
(TWS/IB-Gateway-API) handelt. Kein Signal-Geber fuer einen Menschen, kein Backtest-Spielzeug —
am Ende steht eine laufende, autonome Ausfuehrung. Alles andere in diesem Dokument (Fundament,
offene Punkte, Backtesting) ist **Unterbau fuer dieses eine Ziel**, keine eigenstaendigen Ziele.

Status: **Planungsphase, kein Code.** Der Weg dahin fuehrt ueber echte, wachsende Datenbasis
statt ueber vorschnelle Regeln: aus den taeglich wachsenden OHLC-Daten in `raw/marktdaten/`
einen regelbasierten Handelsalgorithmus fuer MNQ ableiten, der sich per IBKR-API selbststaendig
ausfuehrt.

## Datengrundlage

- `raw/marktdaten/<jjjj>/<mm>/<dd.mm.jjjj>/` — ein Ordner pro Handelstag, TradingView-Exporte in
  1m/5m/15m/1h/4h/1d. Jahr/Monat verschachteln sich von selbst beim Wechsel, damit der Ordner
  bei taeglichen Exporten nicht flach mit hunderten Tagesordnern volllaeuft. Konvention:
  [[OHLC-Datenanalyse (Workflow)]].
- Ab jetzt taeglich neu: TradingView-Export flach nach `raw/marktdaten/` legen,
  `tools/sort_marktdaten.py` raeumt automatisch ein (laeuft auch vor jedem `push.ps1` und
  vor jedem `analyze_ohlc.py`-Aufruf).
- Bisher eingesammelte Tage: siehe Log unten.

## Bereits vorhandenes Fundament

`tools/analyze_ohlc.py` rechnet pro Tag bereits:

- Opening Prices (Midnight/8:30/9:30/13:30), Session-Level (Asia/London/NY AM/PM/...)
- Liquidity Sweeps (mit Levelalter + Penetration + Bestaetigungsfenster)
- Market Structure Breaks (BOS/MSS, sequenziell)
- Displacement-Kerzen, Fair Value Gaps (Groesse, CE, Fuellstatus)
- Macro-Fenster-Expansion (Jannes' XX:50–XX+1:10-Raster)
- Consolidation-Phasen, unangetastete Liquiditaet am Datenende
- Setup-Checkliste gegen einen Zeitpunkt (`--at HH:MM`), 7 von 8 Punkten objektiv pruefbar

Das ist die Rohmasse fuer den Algorithmus — noch keine Handelslogik, nur Erkennung.

## Was fuer einen Algorithmus noch fehlt

1. **Backtesting-Harness. Erste Version existiert:** `algo/backtest_ohlc.py`. Laeuft ueber
   alle Handelstage in `raw/marktdaten/`, importiert dieselben Detektoren aus
   `tools/analyze_ohlc.py` (`fvgs`, `viis`, `sweeps`, `structure_breaks`, `macro_windows` —
   keine Neuimplementierung) und schreibt die Aggregation nach
   `wiki/synthesis/Muster-Validierung (laufend).md`. Bleibt bewusst Standardbibliothek wie
   `analyze_ohlc.py` selbst — die Aggregation ist eine Handvoll Zaehler, kein
   DataFrame-Problem. **Wichtige Falle, auf die man achten muss:** `analyze_ohlc.py` skaliert
   `min_age`/`confirm` beim CLI-Aufruf mit dem Timeframe (in `main()`); wer die Detektoren
   direkt importiert, muss dieselbe Skalierung von Hand anwenden, sonst laufen Tagesreport
   und Backtest mit verschiedenen Schwellen und liefern nicht vergleichbare Zahlen (genau
   dieser Fehler ist beim ersten Entwurf passiert und wurde noch am selben Tag gefixt).
   **Werkzeugentscheidung pandas/matplotlib** bleibt fuer den Explorations-/Visualisierungs-
   Layer (`algo/viz_prototype.py` + `.html`, Zeitzonen/Fenster-Slicing sind dort eine Zeile
   statt einer Schleife) — beide Wege liefern dieselben Zahlen (Macro-Range 175,00 Pkt
   deckungsgleich). Abhaengigkeiten fuer den pandas-Pfad: `algo/requirements.txt`.
   **Naechster Ausbauschritt (Nutzerwunsch "alle PD Arrays, das gesamte Wiki"):** siehe
   Abdeckungs-Tabelle in der generierten Seite selbst — Order-Block-Varianten, IFVG, BPR,
   CBDR, NWOG/NDOG, OTE, Breakaway Gap, Suspension Block, Judas-Swing-Zeitfenster und
   SMT (zweites Symbol noetig) haben noch keinen Detektor.
2. **Harte Entry-/Exit-/Stop-/Target-Regeln.** Die 8er-Checkliste ist eine manuelle
   Pruefliste fuer einen Menschen ("Entry bleibt deine Entscheidung"). Ein Algorithmus
   braucht stattdessen eine deterministische Regel, die aus denselben Daten einen
   Trigger + Stop + Ziel ableitet.
3. **Stichprobengroesse.** Aktuell liegen 2 Handelstage vor (31.07., 03.08.2026). Fuer
   belastbare Aussagen zu Killzone-Trefferquote, Macro-Expansion-Rate oder
   Silver-Bullet-Fenstern braucht es Wochen bis Monate. Vor Regel-Festlegung: weiter
   sammeln, nicht auf 2 Tage ueberfitten.
4. **Risk-/Money-Management.** Positionsgroesse, Stop-Distanz relativ zur gesweepten
   Liquiditaet, Max-Trades/Tag — bisher nirgends im Vault spezifiziert.
5. **Pattern-Prioritaet.** Kandidaten aus dem Wiki, sobald genug Tage vorliegen:
   [[Silver Bullet Model]], [[NY Lunch Macro Model]], [[Judas Swing]] / AM-Reversal,
   [[Midnight Opening Range]] STD-Projektion, [[ORG (Opening Range Gap) & 1st Presented FVG]].

## Code-Ideen (Backlog — notiert, noch nicht umgesetzt)

Ideen werden hier gesammelt, sobald sie auftauchen, statt verloren zu gehen oder verfrueht
implementiert zu werden. Kein Punkt hier ist beauftragt — erst wenn genug Tage vorliegen
(siehe oben), wird priorisiert und gebaut.

1. ~~`tools/backtest_ohlc.py`~~ **erledigt, siehe oben** — liegt unter `algo/backtest_ohlc.py`
   statt `tools/`, sonst wie geplant: laeuft ueber alle Tagesordner, aggregiert die
   bestehenden Detektoren, schreibt nach `wiki/synthesis/Muster-Validierung (laufend).md`.
   Nutzer wollte es sofort mit den vorhandenen 2 Tagen starten statt auf 20-30 Tage zu
   warten — die Seite selbst warnt jetzt explizit vor der kleinen Stichprobe, statt zu warten.
2. **Regel-Schicht statt Checkliste — erste Regel steht:** `algo/rules.py`, `plan_trade(bars,
   when) -> TradeSetup | None`. Setzt das [[Silver Bullet Model]] um (drei Zeitfenster,
   FVG im Fenster als Trigger, Entry = FVG-C.E., Stop = FVG-Gegenkante + Puffer, Ziel = naechstes
   Level aus `untouched_levels()`). Laeuft nur auf `bars[t<=when]` (kein Lookahead). Smoke-Test
   gegen alle 26 Juli-Tage: erzeugt an einem Tag ein Setup, an den anderen korrekt `None`
   (meist weil keine unberuehrte Zielliquiditaet vorliegt) — noch nicht gegen viele Tage
   validiert, siehe Stichprobengroesse unten. Noch offen: nur die Basisregel aus dem Wiki
   (Fenster + FVG + Ziel), die dort genannte zusaetzliche Confluenz (NWOG/NDOG, Midnight
   Opening Fibs, 1. presented FVG) fliesst noch nicht ein; der "2022 Entry" (FVG als IFVG in
   Gegenrichtung, siehe [[Silver Bullet Model]]) ist eine Variante, keine Pflicht, und bewusst
   nicht die erste Version. **Trade-Simulation steht:** `algo/backtest_bt.py` verdrahtet `plan_trade()` in eine
   `backtesting.Strategy` (PyPI-Bibliothek `backtesting`, kein Eigenbau fuer Equity/Drawdown/
   Bracket-Orders). Erster Lauf ueber alle 26 Juli-Tage (5m, MNQ): 53 Trades, Win Rate 15%,
   Profit Factor 0,32, Expectancy -0,046%/Trade — die Basisregel ohne Confluenz verliert auf
   dieser (kleinen) Stichprobe. Zwei Einschraenkungen vor jeder Interpretation: (1) `backtesting`
   preist wie eine Aktie (Notional * Commission-Rate, keine MNQ-Punktwerte) — die $-Kennzahlen
   (Equity, Commissions, Sharpe) sind daher keine echte MNQ-P&L, nur die Prozent-/Trade-Kennzahlen
   (Win Rate, Profit Factor, Expectancy) sind aussagekraeftig; fuer echte $-P&L braeuchte es eine
   eigene Auswertung von `stats._trades` mit Punktwert $2/Punkt statt `stats.Equity Final`.
   (2) Der Stop-Puffer (10% der FVG-Groesse) ist oft kleiner als das Rauschen einer einzelnen
   5m-Kerze — `backtesting` markiert dutzende Trades explizit als "dubious", weil SL/TP in
   derselben Kerze wie der Entry ausgeloest wird (Intra-Kerze-Reihenfolge unbekannt). Vermutlich
   Hauptursache fuer die niedrige Win Rate, noch nicht behoben. **Stop-Puffer-Test erledigt**
   (siehe Log 2026-08-05, `algo/backtest_walkforward.py`) — Vergroessern hilft, bleibt aber in
   jeder getesteten Groesse verlustreich. Naechster Schritt vor einer echten Bewertung:
   Confluenz-Filter (NWOG/NDOG, Midnight Opening Fibs, 1. presented FVG) ergaenzen.
3. **IBKR-Anbindung als eigener, duenner Adapter** (`algo/broker_ibkr.py`, noch nicht
   angelegt): Order-Ausfuehrung ueber die Interactive-Brokers-API (TWS/IB-Gateway, z.B. per
   `ib_insync` oder offiziellem `ibapi`) hinter einer schmalen Schnittstelle
   (`place_order`, `get_position`, `cancel`), damit die Regel-Schicht broker-unabhaengig
   bleibt. Erst nach Punkt 1+2, und zuerst gegen ein Paper-Trading-Konto, nie direkt live.
4. ~~**Backtest-Ergebnisse als Datenartefakt**~~ **erledigt 2026-08-07** -- `algo/results/<name>.json`
   pro Skript (Ausnahme `backtest_seasonal.py`, siehe `algo/seasonal_tendency.json`), siehe
   Log-Eintrag unten. Urspruenglicher Text: eine Zeile pro erkanntem Setup (Datum, Zeit, Muster,
   Richtung, Entry/Stop/Target, Ausgang) in einer CSV/JSON unter `algo/`. Das ist die Bruecke,
   ueber die "laufende Daten + Wiki verbessern den Algo" konkret wird -- sonst bleibt der Satz
   eine Absicht ohne Mechanismus.
5. Detektor-Schwellen (`CFG` in `analyze_ohlc.py`: `min_age`, `min_pen`, `disp_factor`,
   `confirm`) bleiben die **eine** Quelle fuer Parameter, die Tagesreport und spaeterer
   Backtest teilen — nicht doppelt pflegen.
6. **Live-Status-Loop — umgesetzt:** `algo/live_status.py` + `.claude/commands/algo-live-status.md`
   + `/loop 10m /algo-live-status` (Design: `docs/superpowers/specs/2026-08-04-algo-live-status-loop-design.md`,
   Plan: `docs/superpowers/plans/2026-08-04-algo-live-status-loop.md`). Zieht den laufenden
   Handelstag alle 10 Minuten per yfinance nach `algo/live/<datum>/` (transient, ueberschreibend,
   `raw/marktdaten/` bleibt unangetastet), laesst dieselben Detektoren wie `backtest_ohlc.py`
   auf den 5m-Daten laufen plus `plan_trade()`, und meldet nur *neue* Ereignisse seit dem
   letzten Zyklus. Session-gebunden (`/loop`), kein Cloud-Schedule (Mindest-Takt dort 1h,
   keine lokale Dateizugriff) — Start/Stop manuell per Zuruf.
7. ~~**Bar-Renditen statt nur Trade-Renditen.**~~ **erledigt 2026-08-11** — `algo/confidence.py`
   (`bar_metrics`/`print_bar_metrics`) in `backtest_bt.py` verdrahtet, siehe Log unten. *(Werkzeug
   stand: `algo/masters.py::bar_returns_from_trades()`; offen war nur noch das Verdrahten.)* Aus dem
   Masters-Ingest 2026-08-08
   ([[../wiki/concepts/Profit pro Bar vs. pro Trade|Profit pro Bar vs. pro Trade]]): alle
   `algo/`-Reports rechnen ueber die `backtesting`-Lib auf Trade-Basis. Profit Factor und
   Sharpe sind dort systematisch extremer als auf Bar-Basis — Masters' Beispiel: derselbe
   Sachverhalt ergibt Trade-basiert einen Profit Factor von unendlich und Bar-basiert 1,01.
   Konkret: `stats._trades` um eine Bar-fuer-Bar-Mark-to-Market-Serie ergaenzen (fuer Bars mit
   offener Position) und Kennzahlen zusaetzlich darauf rechnen. **Voraussetzung fuer die
   Punkte 8-10.**
8. ~~**Guard Buffer im Walk-Forward.**~~ **geprueft + gehaertet 2026-08-11.** Befund: kein Leck.
   `signals.py` ist zustandslos (reine Funktionen, `history = mnq_rows[:i+1]`, kalenderbasierte
   Signale strikt `< target_day`), und die Ensemble-Zielgroesse ist die Richtung des Folgetags
   (`build_features`: `y[i]` = Tag i+1), also Lookahead genau 1 -> `guard_buffer(L,1)=0`.
   SilverBulletStrategy entscheidet pro Kerze nur aus `bars[t<=when]` in einem harten 1h-Fenster,
   kein tagesuebergreifender Zustand. Die adjazenten Folds in `validate.py` lecken damit nicht.
   Trotzdem gehaertet: `walk_forward(..., omit=0)` streicht bei omit>0 die juengsten
   Trainingstage je Fold — Default 0 (byte-identisch), aber ein spaeter verlaengerter Zielhorizont
   H muss nur noch `omit=H-1` setzen, statt still anti-konservativ zu werden. Aus
   [[../wiki/concepts/Walk-Forward Guard Buffer & Varianz-Inflation|Walk-Forward Guard Buffer & Varianz-Inflation]].
   Masters' Zahl zur Groessenordnung: ohne Puffer erreicht ein wertloses System auf
   Random-Walk-Daten t = 74,64 statt 0.
9. ~~**Konfidenz-Untergrenze und ehrliche Drawdown-Grenze.**~~ **erledigt 2026-08-11.** (a) BCa-
   Untergrenze fuer mittlere Bar-Rendite und Profit Factor jetzt im `backtest_bt.py`-Report
   (`confidence.py`, PF ueber `exp(BCa auf log PF)`). (b) `validate.py::monte_carlo` weist die
   Drawdown-Zeile jetzt doppelt aus: die alte Perzentil-Zeile als "naiv" markiert, darunter die
   korrekte `double_bootstrap_drawdown()` (dd_conf 0,95/0,99) ueber `masters.drawdown_bound`.
   Siehe Log unten.
   [[../wiki/concepts/Konfidenzgrenzen für Renditen (t-Test, Bootstrap, BCa)|Konfidenzgrenzen für Renditen]]
   und [[../wiki/concepts/Grenzen für Einzelrenditen & Drawdown|Grenzen für Einzelrenditen & Drawdown]].
   Zwei getrennte Luecken: (a) Reports weisen nur Punktschaetzer aus — eine BCa-Untergrenze
   (`scipy.stats.bootstrap(method="BCa")`) zeigt, ob die Zahl ueberhaupt von null
   unterscheidbar ist; beim Profit Factor den **Logarithmus** bootstrappen, sonst versagt es.
   (b) Das Monte-Carlo-Trade-Resampling in `validate.py` ist exakt der von Masters als
   "incorrect" bezeichnete naive Drawdown-Bootstrap — er unterschaetzt bei kleiner Stichprobe
   Katastrophen-Drawdowns um bis zu Faktor 13,65. Vor jeder Kapitalentscheidung auf den
   Doppel-Bootstrap umstellen.
10. **Return-Partitionierung + Selection-Bias-P-Wert im geplanten `permutation_test.py`.**
    [[../wiki/concepts/Return-Partitionierung (Skill, Trend, Training Bias)|Return-Partitionierung]]:
    `TotalReturn = Skill + Trend + TrainingBias`, wobei `Trend = (NumLong − NumShort) ×
    TrendPerReturn` direkt berechenbar ist und `TrainingBias` als Mittelwert ueber die ohnehin
    laufenden Permutationen abfaellt. Beantwortet die bisher offene Frage, wie viel eines
    `algo/`-Backtestergebnisses schlicht daran liegt, dass MNQ im Datenzeitraum gestiegen ist.
    Dazu der `unbiased_pval` (Solo- vs. Bestenvergleich), weil im Vault laufend viele Thesen
    gegen dieselben Daten geprueft werden — siehe
    [[../wiki/concepts/Training Bias & Selection Bias|Training Bias & Selection Bias]].
    **Offen, aber jetzt entblockt (2026-08-11): bewusst NICHT mit 7-9 mitgeliefert.** Die
    Werkzeuge stehen (`masters.partition_return`, `permute_bars`, `stoc_bias`), aber
    `permutation_test.py` braucht je Permutation eine **volle Reoptimierung** der Strategie. Die
    dafuer noetige Performance-Voraussetzung ist **erledigt** (siehe Log 2026-08-11): `next()`
    baut die Historie jetzt inkrementell auf (`backtest_bt.extend_hist`, O(n²)->O(n)), ein voller
    50-Tage-Lauf faellt von ~270 s auf ~40 s. Damit sind hunderte Permutationslaeufe praktikabel.
    Naechster Schritt ist also der Bau von `permutation_test.py` selbst.

### Backlog: drei quantifizierte Regeln aus Core Content Month 09 (2026-08-10)

Aus dem Ingest der Month-09-Videoreihe stammen drei Thesen, die konkret genug fuer einen eigenen
Backtest gegen `raw/marktdaten/` sind. Noch nicht umgesetzt — hier als Auftrag notiert.

1. **London-Close-Retracement (`algo/backtest_london_close.py`)** — Bedingung: NY und London
   liefen in dieselbe Richtung **und** die 5-Tage-ADR wurde erreicht/ueberschritten. Erwartung:
   Retracement von 20–30 % der Tagesrange. ⚠️ ICT nennt in Month 09 **zwei abweichende
   Parametersaetze** — Reversal-Lektion: Fenster 10:00–12:00 NY, ~20 %, ADR-Ueberschuss
   1,25–1,33×; Bread-&-Butter-Lektionen: Fenster 10:30–13:00 NY, 20–30 %, kein fester Faktor.
   **Beide Varianten getrennt testen**, nicht eine willkuerlich waehlen. Siehe
   [[../wiki/concepts/Average Daily Range (5-Tage-ADR)|Average Daily Range (5-Tage-ADR)]].
2. **33-Pip-Protraction** — auf einem "klassischen 100-Pip-Tag" laeuft die Protraction unter den
   Midnight-NY-Opening-Price bis zu ~33 Pips, also rund **ein Drittel der erwarteten Tagesrange**.
   Auf MNQ als Verhaeltnis (nicht als Pip-Wert) zu pruefen: Wie tief unter dem Midnight-Open liegt
   das Tagestief an Up-Close-Tagen, relativ zur ADR? Siehe
   [[../wiki/models/ICT Day Trade Routine|ICT Day Trade Routine]].
3. **Filling The Numbers (vier Level)** — IPDA handelt pro Tag zu vier Leveln, gemessen ueber
   CBDR-STDs, Asia-Range-STDs, Pivots **oder** den Flout (Range 15:00–00:00 NY, halbiert). Gut
   testbar, weil CBDR- und Asia-Range-Detektoren bereits existieren; der Flout waere neu. Frage an
   die Daten: Wird die Vier-Level-Faustregel getroffen, und **welche** Messlatte trifft auf MNQ am
   haeufigsten? Siehe
   [[../wiki/concepts/Filling The Numbers (4 Level pro Tag)|Filling The Numbers (4 Level pro Tag)]].

### Backlog: Macro-Messmethodik korrigieren (2026-08-10, aus ICT-Gems-Ingest)

**Hoch priorisiert, weil es eine bereits publizierte Zahl betrifft.** ICT sagt woertlich, das
Macro-Fenster sei ein **Startfenster**, kein Container: *"the move **begins** in those 20 minutes,
it's not the entirety of the move"* (siehe
[[../wiki/sources/youtube/ICT Gems - Blending Silver Bullets and Macros (Source)|Blending Silver Bullets and Macros (Source)]]).

`algo/backtest_macro.py` misst aber Range, Netto und `dir` **innerhalb** des 20-Minuten-Blocks.
Ein Macro, das um 10:05 einen Lauf startet, der bis 10:40 traegt, wird dadurch als schwacher Block
gewertet — die Methode unterschaetzt den Effekt systematisch.

1. **Neue Kennzahl**: Exkursion **ab Macro-Start ueber die folgenden N Minuten** (MFE/MAE-artig,
   N z.B. 20/40/60) statt Blockinhalt. Kontrollbloecke identisch behandeln, sonst entsteht ein
   neuer Confounder.
2. **Letzte Handelsstunde ausnehmen**: Dort gilt das `:50-:10`-Raster laut zwei Quellen **nicht**
   (15:15-15:45 Final Hour Macro, 15:45-16:00 MOC, in der Earnings-Saison zusaetzlich 16:01 und
   16:15). Das Skript rastert aktuell durchgehend gleichmaessig und misst diese Stunde damit falsch.
3. **Spooling-Suche einstellen wie bisher geplant**: Die Suche nach einer volumenbasierten
   Spooling-Definition ("enge Kerzen bei steigendem Volumen") zielte am Begriff vorbei — ICT
   bezeichnet mit Spooling den **gerichteten Lauf zur Liquiditaet**, nicht die Kompression davor.
   Die passende Messgroesse ist der bereits erhobene Nettoweg/`dir`, nicht Volumen (das im Bestand
   ohnehin fehlt).
4. **Mindestziel als Filter pruefen**: ICT nennt **10 Handles** als Untergrenze fuer NASDAQ-Scalps
   und **5 Handles** speziell fuer den Silver Bullet. Auf MNQ direkt als Punktschwelle testbar.

### Backlog: Vier-FVG-pro-Stunde-These (2026-08-10, aus ICT-Gems-Ingest)

ICT behauptet, in **jedem** Viertelstunden-Fenster bilde sich ein FVG auf 15M oder 5M — also
**vier pro Stunde** ("high frequency trading algorithmically"). Wichtig: Das FVG muss sich nur
**bilden**, es muss nicht angehandelt werden. Bleiben sie aus, ist das laut ICT die Definition von
**High Resistance Liquidity Run** und damit sein "number one premise" fuer Nicht-Handeln.

Direkt gegen `raw/marktdaten/` pruefbar und auf `algo/backtest_macro.py`s FVG-Zaehlung aufsetzbar
(die zaehlt bereits FVGs je 20-Minuten-Block mit `--min-fvg`):

1. **Grundfrage**: Wie oft enthaelt ein Viertelstunden-Fenster (:00-:15, :15-:30, :30-:45, :45-:00)
   mindestens ein 1m-FVG? Getrennt nach Mindestgroesse, analog zur bestehenden Tabelle.
2. **Verknuepfung mit dem HRLR-Kriterium**: Sind Stunden **ohne** FVG in mehreren aufeinander-
   folgenden Vierteln messbar anders (kleinere Range, niedrigeres `dir`) als Stunden mit? Das waere
   ein datenseitiger Test des "sit still"-Kriteriums.
3. Vorsicht bei der Interpretation: Die bestehende Messung zeigt bereits, dass **95 % aller
   Macro-Fenster mindestens ein FVG >= 2 Punkte** enthalten — die These koennte also trivial wahr
   und als Filter wertlos sein. Genau das ist zu quantifizieren, statt sie zu bestaetigen.
   Siehe [[../wiki/concepts/Algorithmic Price Delivery Continuum|Algorithmic Price Delivery Continuum]].

### Backlog: zwei Fenster-Widersprueche aus ICT-Quellen (2026-08-10)

Beide Male widerspricht ICT sich selbst zwischen zwei Quellen; **beide Varianten testen**, keine
willkuerlich waehlen:

- **MOC-Fensterlaenge**: 15:50-16:00 (2026er Chronicles) vs. **15:45-16:00** (2024er Gems,
  ausdruecklich *"it's not 10 minutes, it's 15 minutes"*). Siehe
  [[../wiki/models/Market on Close (MOC) Macro Model|Market on Close (MOC) Macro Model]].
- **Lunch-Macro-Start**: Execution im Macro 10:50-11:10 (Lecture 2025) vs. **11:30** als Beginn mit
  Fenster bis 13:30 (Gems 2024). Die 11:30-Fassung ist mechanisch formuliert und damit direkt
  codierbar: erstes Low rueckwaerts von 11:30, das nach 10:00 entstanden ist. Siehe
  [[../wiki/models/NY Lunch Macro Model|NY Lunch Macro Model]].

### Backlog: ORG-Gap nach Groesse segmentieren (2026-08-12, aus Daily-Bias-Notiz)

Jannes' These (aus `raw/daily bias 12.08.md`): bei einem **sehr grossen** ORG-Gap greift die
70%-C.E.-Regel nicht (kein Fill innerhalb 30 Min). `algo/backtest_org_ce.py` erweitern, um die
Fuellrate nach Gap-Groesse (Quartile) statt nur aggregiert auszuweisen — prueft, ob grosse Gaps
die 35–43%-Gesamtquote nach unten ziehen. Siehe [[../wiki/synthesis/Muster-Validierung (laufend)|Muster-Validierung (laufend)]], Nachtrag 2026-08-12.

### Backlog: zwei quant-finance-Thesen aus MIT-15.S08-Batch-2-Ingest (2026-08-12)

Aus dem Ingest der 9 restlichen MIT-15.S08-Transkripte (Lectures 12/14/18-21/23-25, siehe
`wiki/log.md`) stammen zwei konkret codierbare, noch nicht implementierte Thesen:

- **Autokorrelations-These (Box-Pierce)**: prueft, ob MNQ-Returns auf einem oder mehreren
  Timeframes (1m/5m/15m/1h/1d) signifikante Autokorrelation zeigen (`BP = T·Σ R̂_j²`,
  χ²-verteilt). Siehe [[../wiki/concepts/Zeitreihenanalyse für Finance|Zeitreihenanalyse für
  Finance]]. Noch kein Code — Kandidat `algo/backtest_autocorrelation.py`.
- **GARCH(1,1)-Sizing-These**: dynamische Stop-Distance/Positionsgroesse ueber eine laufend
  aktualisierte GARCH-Volatilitaetsschaetzung statt festem ATR-Multiplikator; Datenbedarf
  (OHLC) ist mit `raw/marktdaten/` bereits gedeckt. Siehe
  [[../wiki/concepts/Volatilitätsmodelle (GARCH & Co)|Volatilitätsmodelle (GARCH & Co)]]. Noch
  kein Code — Kandidat `algo/backtest_garch_sizing.py`.

Beide sind reine Backlog-Eintraege gemaess [[Algo-Trading: Arbeitsstandards]] ("jede neue These
wird automatisch geloggt") — Backtest folgt in einer kuenftigen Session, nicht Teil dieses
Ingest-Auftrags.

### Backlog: Risikomanagement-Prinzipien aus Yale-Econ-252-Ingest (2026-08-12)

Aus dem Ingest der 23-teiligen Yale-"Financial Markets"-Playlist (Robert Shiller, siehe
`wiki/log.md` und die neuen `wiki/concepts/*Yale Econ 252*`-Seiten) stammen sechs Punkte mit
direktem Bezug zu Korrektheit/Risiko im eigenen Stack — priorisiert nach Aufwand/Nutzen:

1. **Korrelationsbruch in Stressphasen (hoechste Prioritaet, geringer Aufwand).** These: Setups,
   die im Normalbetrieb unkorreliert wirken, laufen in volatilen Phasen synchron (AIG-Fallstudie,
   siehe [[../wiki/concepts/Versicherung als Risikomanagement-Institution (Yale Econ 252)|Versicherung
   als Risikomanagement-Institution]]). Betrifft direkt `algo/backtest_ensemble.py`, das mehrere
   Regeln kombiniert, ohne die Korrelation ihrer Trade-Returns bedingt auf Vola-Regime zu pruefen.
   Kandidat: `algo/backtest_ensemble.py` um eine Kennzahl "Korrelation der Strategie-Returns,
   getrennt nach oberem/unterem ATR-Quartil" erweitern — warnt, falls die Diversifikations-Annahme
   des Ensembles nur im ruhigen Regime gilt.
2. **Fat-Tail-Realitaetscheck (hohe Prioritaet, geringer Aufwand).** 1987 (-20,47 % an einem Tag)
   und der Doppelcrash 1929 als historische Belege gegen Normalverteilungs-Annahmen (siehe
   [[../wiki/concepts/Value at Risk, CoVaR & Unabhängigkeitsannahme (Yale Econ 252)|Value at Risk,
   CoVaR & Unabhängigkeitsannahme]]). Ergaenzt die bestehende Doppel-Bootstrap-Drawdown-Schaetzung
   in `validate.py`/`stress_test.py` um einen expliziten Hinweis, dass Bootstrap aus der eigenen
   Historie Tail-Ereignisse jenseits des beobachteten Fensters strukturell unterschaetzt — kein
   Code-Fix, sondern eine Reporting-Ergaenzung ("worst observed" vs. "plausibles Tail-Risiko").
3. **Sharpe-Ratio-Manipulierbarkeit durch Tail-Verkauf (mittlere Prioritaet).** Formal belegt
   (Fallbeispiel Integral Investment Management): eine unauffaellig glatte Equity-Kurve mit hoher
   Sharpe-Ratio kann aus verkauften Tail-Optionen/kurz gehaltenen Crash-Risiken stammen, nicht aus
   echtem Edge. Bezug zur bestehenden `dubious_pct`-Pflichtkennzahl (siehe
   [[Algo-Trading: Arbeitsstandards]]) — ergaenzt sie um eine Warnung, wenn Sharpe hoch UND
   Drawdown-Verteilung stark linksschief ist (Skewness-Check in `selfcheck.py`).
4. **Gambler's-Ruin-Bestaetigung (kein neuer Code).** Yale-Formel `[(1−p)/p]^S` fuer
   Market-Maker-Ruin ist mathematisch identisch zur bereits im Vault genutzten Kelly-Ruin-Grenze —
   reine Bestaetigung, kein Handlungsbedarf.
5. **Random-Walk-vs.-AR(1) (kein neuer Code).** Liefert die oekonomische Begruendung fuer den
   bereits eingesetzten Monte-Carlo-Permutationstest — reine Bestaetigung.
6. **Futures-Fair-Value-Formel fuer Rollover (niedrige Prioritaet).** Contango/Backwardation-Logik
   (siehe [[../wiki/concepts/Forward- & Futures-Märkte (Contango, Backwardation, Yale Econ 252)|Forward-
   & Futures-Märkte]]) erklaert strukturell den MNQ/NQ-Rollover-Sprung in den Rohdaten — relevant,
   sobald Kontraktwechsel in `raw/marktdaten/` explizit behandelt werden (aktuell kein bekanntes
   Problem, daher niedrige Prioritaet).

Alle sechs sind Backlog-Eintraege gemaess [[Algo-Trading: Arbeitsstandards]] — Umsetzung folgt in
einer kuenftigen Session, nicht Teil dieses Ingest-Auftrags. Punkt 1 und 2 zuerst, da sie
bestehende Validierungsluecken direkt schliessen (Korrektheit vor Features).

## Naechster Schritt

**Korrektur (2026-08-03):** Der urspruengliche Plan war, mit dem Backtest zu warten, bis
genug Tage vorliegen. Nutzerwunsch war ausdruecklich das Gegenteil — sofort mit den
vorhandenen Daten anfangen und bei jedem neuen Tag weiterlaufen lassen, damit sich die
Aussagekraft von selbst aufbaut, statt eine willkuerliche Wartegrenze abzuwarten. Umgesetzt:
`algo/backtest_ohlc.py` existiert und wird ab jetzt bei jedem neuen Handelstag erneut
ausgefuehrt (siehe [[../wiki/models/OHLC-Datenanalyse (Workflow)|OHLC-Datenanalyse (Workflow)]]).
Jeder neue Tag bekommt weiterhin zusaetzlich seinen eigenen
`wiki/synthesis/MNQ <Datum> — Datenbasierter Tagesrueckblick.md`; die Muster-Validierungs-Seite
aggregiert alle Tage zusammen. Naechster Ausbauschritt ist mehr Detektoren (siehe Punkt 1
oben), nicht mehr Warten.

## Log

| Datum | Ereignis |
|---|---|
| 2026-08-03 | Projekt angelegt. Datengrundlage: 31.07.2026, 03.08.2026 (03.08. Handelstag noch nicht beendet, Daten bis 16:18 NY). Bug in `tools/analyze_ohlc.py` gefixt: HTF-Kontext ("Vortag") behandelte die noch laufende Tageskerze faelschlich als Historie. |
| 2026-08-03 | Nutzer praezisiert das Ziel: **Schicht 1 = autonomer IBKR-Handelsalgorithmus**, alles andere hier ist Unterbau. `raw/marktdaten/` auf Jahr/Monat-Verschachtelung umgestellt (`tools/sort_marktdaten.py`, `tools/analyze_ohlc.py` mitgezogen); bestehende Tage nach `2026/07/31.07.2026` bzw. `2026/08/03.08.2026` migriert. Erste Code-Ideen als Backlog notiert (Backtest-Harness, Regel-Schicht, IBKR-Adapter, Backtest-Ergebnis-Artefakt). |
| 2026-08-03 | Frage beantwortet: pandas eignet sich fuer den Backtest-/Aggregations-Layer (kein eigenes Backtesting-Framework, aber die richtige Grundlage fuer Punkt 1 oben). pandas + matplotlib installiert. Beleg gebaut: `algo/viz_prototype.py` laedt den 03.08.-5m-Chart per pandas, `algo/viz_prototype.html` zeigt ihn interaktiv (Preislinie, Sweep/Displacement/Macro-Expansion annotiert, Hover-Tooltip) — Macro-Range 175,00 Pkt deckt sich exakt mit `tools/analyze_ohlc.py`. `algo/requirements.txt` neu (pandas, matplotlib; `tools/analyze_ohlc.py` bleibt Stdlib-only). |
| 2026-08-03 | Nutzerwunsch: alle Daten (CISD, ORG, "alle PD Arrays", "das gesamte Wiki") sollen jetzt schon und laufend gegen echte Daten geprueft werden, konkrete Frage: "wird das C.E eines ORG zu 70% gefuellt?". Ergebnis: dieser 70%-Wert steht in keiner ingesteten Quelle im Vault (gezielt gesucht, nur False-Positives zur "70%-Wednesday-Regel" gefunden) — vermutlich allgemeines ICT-Wissen von aussen, nicht aus dem hier vorliegenden Material. `algo/backtest_ohlc.py` gebaut: aggregiert `fvgs`/`viis`/`sweeps`/`structure_breaks`/`macro_windows` ueber alle Tagesordner, schreibt `wiki/synthesis/Muster-Validierung (laufend).md`. **Bug im ersten Entwurf gefunden und gefixt**: CFG-Skalierung (`min_age`/`confirm` je Timeframe) wird nur in `analyze_ohlc.py`s CLI (`main()`) angewendet, nicht wenn man die Detektoren direkt importiert — dadurch waren Struktur-Break-Zahlen vor dem Fix ca. 5x zu niedrig. Neuer Detektor `viis()` in `tools/analyze_ohlc.py` (Volume Imbalance, 2-Kerzen-Luecke Close→Open) ergaenzt, als erster Schritt Richtung "alle PD Arrays" — vollstaendige Liste der noch fehlenden PD Arrays (Order-Block-Varianten, IFVG, BPR, CBDR, NWOG/NDOG, OTE, Breakaway Gap, Suspension Block, Judas-Zeitfenster, SMT) in der generierten Seite und hier im Backlog festgehalten, nicht in einem Schritt geraten. Ergebnis bei n=2 Tagen (**nicht belastbar**, nur Statusbericht): C.E erreicht bei grossen FVGs 78%, komplett gefuellt 67% — in der Naehe der kursierenden 70%, aber bei 2 Tagen reiner Zufall moeglich. |
| 2026-08-04 | `yfinance` installiert (`algo/requirements.txt`), `algo/fetch_yfinance.py` gebaut: laedt MNQ=F (5m/15m/1h/1d + aus 1h resampled 4h) und legt die Daten im selben Format wie die TradingView-Exporte ab. Kompletten Juli 2026 nachgeladen (26 Handelstage, vorhandene 31.07.-Exporte nicht angetastet — Datei existiert bereits -> ueberspringen statt ueberschreiben). Nutzer betont: **Zeit hat Prioritaet vor Preis** ("Time before Price", ICT-These eines algorithmisch gesteuerten Preisverlaufs zu bestimmten Uhrzeiten) — Timestamp-Korrektheit deshalb explizit gegen die vorhandene 31.07.-TradingView-Datei gegengeprueft (identische Epochs). Dabei zwei Fehler im ersten Entwurf gefunden und gefixt, bevor Daten uebernommen wurden: (1) erster Lauf ueberschrieb die 5 bestehenden 31.07.-Dateien, aus Git wiederhergestellt und Skript geschuetzt; (2) Pandas 3.0 liefert Datetime64 nicht mehr einheitlich in Nanosekunden, dadurch stand ueberall `time=1` — Konvertierung auf `.as_unit("s")` umgestellt. Zusaetzlich RTH-Varianten (09:30-16:00 NY, Fenster aus dem bestehenden manuellen `15m RTH`-Referenzfile uebernommen) fuer 5m/15m/1h ergaenzt — yfinance hat keinen eigenen RTH-Feed, das ist derselbe Datenstrom nur gefiltert. 1m bewusst ausgelassen: yfinance liefert Minutendaten nur fuer die letzten ~7 Tage. Bekannte Luecken: 03.07. (verkuerzte Feiertagssession) ohne 1d-Kerze, die vier Sonntage im Juli nur mit einem duennen Ausreisser-Bar. |
| 2026-08-04 | Nutzerwunsch: 1m per yfinance nachladen, wo moeglich. `algo/fetch_yfinance.py`: `1m` zu `INTERVALS`/`RTH_TFS` ergaenzt, `download_interval()` fragt 1m in 7-Tage-Haeppchen an (yfinance-Limit pro Request). Backfill fuer 01.07.-04.08. gelaufen: 1m + `1m RTH` fuer 08.07.-31.07. geschrieben (30-Tage-Fenster ab heute), 01.07.-07.07. bleiben leer (Yahoo-Fehler "must be within the last 30 days", erwartet), 31.07./03.08./04.08. unangetastet (TradingView-Originale, `raw/` bleibt unveraenderlich). |
| 2026-08-04 | Nutzerwunsch: `plan_trade()` bauen, als Regel das [[Silver Bullet Model]] nutzen. `algo/rules.py` gebaut (Backlog-Punkt 2): drei Zeitfenster (London 3-4, NY AM 10-11, NY PM 14-15 Uhr NY) aus dem Wiki, erstes im Fenster bestaetigtes FVG als Trigger, Entry=C.E., Stop=Gegenkante+10%-Puffer, Ziel=naechstes `untouched_levels()`-Level in Traderichtung (kein Ziel -> kein Setup). Nutzt nur bestehende Detektoren (`fvgs`, `untouched_levels`), keine Neuimplementierung. Bewusst kein Lookahead: alle Detektoren laufen nur auf `bars[t<=when]`. Selbstcheck mit synthetischen Bars (`python algo/rules.py`) plus Smoke-Test gegen alle 26 Juli-Tage: ein Setup am 01.07., an den uebrigen Tagen korrekt `None` (meist fehlende Zielliquiditaet). Noch nicht drin: die vom Wiki geforderte Zusatz-Confluenz (NWOG/NDOG, Midnight Opening Fibs, 1. presented FVG) und der "2022 Entry" (IFVG-Variante) — bewusst als Basisversion gestartet, siehe Backlog-Punkt 2. `plan_trade()` liefert nur Setups, noch keine Trade-Simulation (P&L/Equity) — dafuer wurde die PyPI-Bibliothek `backtesting` als naechster Baustein besprochen. |
| 2026-08-04 | Live-Status-Loop gebaut: `algo/live_status.py` (Fetch heutiger Handelstag per yfinance nach `algo/live/<datum>/`, Detektoren wie `backtest_ohlc.py` auf 5m + `plan_trade()`, Diff gegen `state.json` fuer neue Ereignisse seit letztem Zyklus) + `.claude/commands/algo-live-status.md` + `/loop 10m /algo-live-status`. Session-gebunden statt Cloud-Schedule (Mindest-Takt dort 1h, kein lokaler Dateizugriff). `algo/live/*/` neu in `.gitignore` (transient, ueberschreibend); die Text-Statusberichte `algo/live/<datum>-status-log.md` bleiben versioniert. |
| 2026-08-04 | Nutzerwunsch: die konkrete 70%-These aus 2026-08-03 (Zeile oben) direkt pruefen: "wird das ORG-C.E. zu 70% in den ersten 30 Minuten (9:30-10:00 NY) gefuellt?" — Nutzer verortet die 70%-Zahl bei ICT/SMC. Neuer Detektor `org_gap()` in `tools/analyze_ohlc.py`: Gap zwischen ~16:14-Schlusskerze des Vortags und 9:30-Open, C.E. = Mittelpunkt, prueft Beruehrung bis 10:00 NY. `algo/backtest_org_ce.py` neu (analog `backtest_ohlc.py`, laedt je Tag Vortag+Tag zusammen, da beide Konventionen — TradingView-Export wie yfinance-Fetch — den "Handelstag" teils erst ab 18:00 Vorabend abgrenzen und die 16:14-Kerze sonst fehlt). Ergebnis ueber alle verfuegbaren Tage mit 1m/5m-Aufloesung (n=26-30 je nach Nachlade-Stand, 2026-06-16 bis 2026-07-31): **C.E.-Fill-Quote 35-43%**, nicht 70%. `algo/fetch_yfinance.py` dabei erweitert: `CHUNK_DAYS` verallgemeinert von nur-1m auf 1m/5m/15m (Bug gefixt: ein einzelner Request ueber >60 Tage fuer 5m/15m schlug bisher komplett fehl statt wenigstens die letzten ~60 Tage zu liefern) — damit 5m-Historie bis 2026-06-15 statt vorher effektiv nur ab 07-08 verfuegbar. Harte Grenze bestaetigt: yfinance liefert 1m nur ~30, 5m/15m nur ~60 Tage zurueck, mehr Jahreshistorie in Intraday-Aufloesung ist darueber nicht zu bekommen. `org_gap()` zusaetzlich in `algo/live_status.py` verdrahtet (`org_ce`-Feld im JSON, sowohl `--dry-run` als auch Live-Betrieb) — Live-Pfad brauchte einen eigenen `org_bars`-Parameter in `run_detectors()`, weil `fetch_today()` den Tag bereits vorab auf `day` filtert und die Vortagskerze dabei verliert. **Nutzer-Entscheidung trotz Ergebnis**: These bleibt als aktiv beobachtete Groesse bestehen (Quelle ICT/SMC), kuenftige Handelstage sollen laufend mitgezaehlt werden statt die Frage als erledigt/widerlegt zu betrachten — `algo/backtest_org_ce.py` waechst automatisch mit, sobald neue Tage per `fetch_yfinance.py` nachgezogen werden. |
| 2026-08-04 | Nutzerthese: sind bestimmte FVGs "besonders" (staerker/verlaesslicher, haelt eher als Support/Resistance) — konkret das 1. FVG nach 9:30 NY (NY-AM-Sessionstart), das 1. FVG nach 0:00 NY (Midnight-/London-ORG-Start) und das 1. FVG jeder neuen Stunde im 1m-Chart? `algo/backtest_fvg_specialness.py` neu: klassifiziert alle `fvgs()`-Treffer ueber alle Tage in `raw/marktdaten/` in die drei (ueberlappenden) Kategorien plus "rest", vergleicht Fuellrate/C.E.-Hit-Rate/Ø-Groesse. Ergebnis (37 Tage, n=34/35/737/5160): **Groesse eindeutig bestaetigt** — alle drei Kategorien deutlich groesser als normale FVGs (22,43 / 11,35 / 10,45 vs. 6,88 Punkte). **"Haelt eher" nur teilweise bestaetigt**: beim 1. FVG nach 9:30 klar (Fuellrate 76,5% vs. 93,0% Baseline, spuerbarer Unterschied), bei Midnight-FVG und Stunden-FVG dagegen nur ein kleiner Effekt (91,4%/90,9% vs. 93,0% — paar Prozentpunkte, kein starker Beleg). Kleine Stichprobe bei den beiden Session-Kategorien (n=34/35, ein Wert pro Tag), Stunden-Kategorie deutlich breiter abgesichert (n=737). |
| 2026-08-04 | **Neue Standard-Regel**: Nutzer will kuenftig JEDE neue These automatisch geloggt + gegen alle Daten gebacktestet sehen, ohne vorher zu fragen ("logst du weiter und backtestet automatisch"). Ab jetzt Standardverfahren fuer dieses Projekt (siehe Merkzettel). |
| 2026-08-11 | **Neue These aus ICT-Executions-Ingest** (Primärquelle: [[2023-12-06 - Why Do I Avoid NFP Weeks NQ 3 Trades 2 Losses (Source)]]): ICT meidet NFP-Wochen (Non-Farm-Payroll-Freitag) wegen erhoehter Verlustrate — im gezeigten Beispiel 2 von 3 Trades verloren. Noch **nicht gebacktestet** (Backlog): braucht NFP-Releasedaten (Kandidat: FRED-Kalender via `algo/fetch_fred.py`, falls dort verfuegbar, sonst externe Liste der ersten Freitage im Monat) verknuepft mit Trade-Ergebnissen aus `algo/backtest_*`-Laeufen, um Win-Rate NFP-Woche vs. Rest zu vergleichen. |
| 2026-08-11 | **NFP-These erster Backtest**: `algo/backtest_nfp_week.py` neu (NFP-Freitag genaehert als erster Freitag im Monat, keine echte BLS-Terminliste verfuegbar). Kein Trade-P&L pro NFP-Tag reproduzierbar (das Executions-Video zeigt reale, nicht regelbasierte Trades) — stattdessen zwei Proxy-Metriken: Range und Whipsaw-Ratio (Range / \|Netto-Bewegung\|). Ergebnis bei n=6 NFP-Freitagen (**sehr kleine Stichprobe, nicht belastbar**): Range **56% hoeher** als an anderen Freitagen (749 vs. 480 Pkt) — bestaetigt "volatiler". Whipsaw-Ratio dagegen **niedriger** (2,5 vs. 4,33) statt hoeher — NFP-Tage sind eher staerker gerichtet als "choppy", was der reinen "schwerer zu halten weil whipsaw"-Lesart widerspricht. Ehrliches Zwischenfazit: hoehere Volatilitaet bestaetigt, aber die Verlustursache aus dem Video (2/3 Trades) laesst sich mit dieser Datenlage nicht auf "mehr Chop" zurueckfuehren — plausibler ist schlicht groesserer Punkte-Stop-Abstand noetig bei gleichbleibender Positionsgroesse. These bleibt offen, n zu klein fuer eine Entscheidung. |
| 2026-08-04 | Nutzerfrage: legt die "London ORG" per Standard-Deviation-Projektion das Tages-High/-Low fest? Praezisierung: gemeint ist die **Midnight/London Opening Range (0:00-0:30 NY)**, nicht das ORG-Gap (16:14->9:30) — beide heissen im Quellenmaterial teils "London Opening Range", sind aber laut [[../wiki/concepts/Midnight Opening Range|Wiki]] zwei verschiedene Dinge. Quelle behauptet: Fib ueber die 0:00-0:30-Range, mit negativen STD (Vielfache der Range-Groesse) nach unten verlaengert, antizipiert Tages-High/-Low; **max. Manipulation waehrend London bei -1 STD**, danach ist es "der echte Move" (keine Grenze mehr). `algo/backtest_midnight_range_std.py` neu: misst k = wie viele Range-Vielfache Tages-High/-Low ueber die Range-Grenze hinausgehen, getrennt fuer London-Fenster (1-5 Uhr) und ganzen Tag. Ergebnis (34 Tage): waehrend London Median 0,79 STD fuers Low (grob konsistent mit "bleibt meist nah an der Range"), aber **50% der Tage ueberschreiten -1 STD trotzdem** — die "harte Grenze" haelt nicht durchgehend. Ganzer Tag geht erwartungsgemaess viel weiter (Median ~4-6 STD) — das widerspricht der These nicht, die Quelle selbst sagt, nach London gilt keine Grenze mehr. **Wichtige Einschraenkung**: der Test kann "Manipulations-Swing" nicht von "echtem Move" unterscheiden (genau die Ausnahme, die die Quelle fuer Ueberschreitungen nennt) — ein rohes Min/Max-Fenster ist dafuer zu grob, ein sauberer Test bräuchte einen Judas-Swing-/Reversal-Detektor als naechsten Schritt. |
| 2026-08-04 | Nutzer praezisiert zwei Punkte: (a) die naive Min/Max-Zahl war nicht gemeint als "immer genau -1 STD", sondern es geht um Median/Muster; (b) die eigentliche These ist woertlicher: **"in den ersten 30 Minuten (0-0:30) wird das High oder Low festgesetzt"** (ICT-Zuschreibung), nicht nur die STD-Projektion. `algo/backtest_midnight_range_judas.py` neu: klassifiziert pro Tag/Seite (Sellside unter Range-Low, Buyside ueber Range-High) in `no_extension` (Seite waehrend London nie durchbrochen), `manipulation` (Durchbruch + Rueckeroberung per Schlusskurs noch in London bis 5 Uhr) oder `trend` (Durchbruch haelt bis 5 Uhr — laut Quelle dann kein Manipulation-Swing mehr). Ergebnis (34 Tage): **Manipulationstiefe bei echten Manipulations-Faellen liegt im Median bei 1,04 STD (Sellside) bzw. 1,10 STD (Buyside)** — sehr nah an der "-1 STD"-Aussage, sobald Trend-Tage rausgerechnet werden (die naive Zahl vorher war durch Trend-Tage nach oben verzerrt). **Woertliche thse "High oder Low in 0:00-0:30 gesetzt"**: an 50,0% der Tage (17/34) wurde waehrend London mindestens eine Seite (High oder Low) gar nicht durchbrochen — deutliche Stuetzung. Nie beide Seiten gleichzeitig (0/34) — es bewegt sich fast immer wenigstens eine Richtung. Sellside haelt insgesamt haeufiger (35,3% no_extension) als Buyside (14,7%) — moeglicher Bias-Hinweis (in diesem Datensatz eher bullische Tage: Low haelt oefter, High wird oefter per echtem Move ueberschritten — 47,1% Trend auf der Buyside). |
| 2026-08-04 | Nutzerthese erweitert: (a) nicht nur die Kerzen-Range, auch das 1./groesste FVG *innerhalb* 0:00-0:30 soll per STD ein High/Low festlegen (Beleg dafuer stand schon im Wiki: "gerade beim 1. presented Displacement werden diese Level respektiert"); (b) jede Session hat ihre eigene Opening Range, nicht nur Midnight/London. Rohquellen-Check bestaetigt (b): **NY Pre-Session ORG = 7:00-7:30 NY** (`MentorShip 2025/06 Opening Range Theory...md`) und **NY PM ORG = 13:30-14:00 NY** (`ICT 2025 Lecture Series/NY Lunch Macro Rules & PM Session...md`), beide explizit mit "1. presented FVG der Session" — NY AM (9:30) bleibt strukturell anders (ORG-*Gap*-Mechanik 16:14->9:30, kein 30-Min-Kerzen-Range wie die anderen drei). STD-Projektion ist in den durchsuchten Rohquellen **nur fuer Midnight/London (und CBDR) explizit belegt**, nicht fuer NY Pre-Session/NY PM — dort nur "1. presented FVG"-Logik ohne STD-Erwaehnung gefunden, also nicht 1:1 als bestaetigt behandelt. `algo/backtest_midnight_range_judas.py` generalisiert (`session_range()` in `backtest_midnight_range_std.py` parametrisiert, `classify_side()` nimmt jetzt ein Testfenster als Parameter statt fest "London"), laeuft jetzt ueber alle drei Sessions plus eine FVG-basierte Variante fuer Midnight. Ergebnis: **Manipulationstiefe clustert bei allen drei Sessions grob im gleichen Bereich wie Midnight/London** (Mediane zwischen 0,37 und 1,10 STD ueber alle 6 Seite/Session-Kombinationen) — dasselbe Grundmuster gilt session-uebergreifend. **"High oder Low in der Opening Range gesetzt"**: Midnight/London 38,2%, NY Pre-Session 51,5%, NY PM 50,0% der Tage — an rund der Haelfte der Tage in allen drei Sessions haelt die These woertlich. **FVG-Variante**: Klassifikations-Raten (Manipulation/Trend/kein Durchbruch) aehnlich wie bei der Kerzen-Range, aber die STD-Tiefe selbst ist **nicht aussagekraeftig** (Median 5-6,5 STD) — FVGs innerhalb der Range sind oft nur wenige Punkte gross, dadurch wird jede normale Tagesbewegung rechnerisch zu "vielen STD", das ist ein Artefakt der kleinen Nenner-Groesse, keine echte Erkenntnis ueber Staerke. |
| 2026-08-04 | `/goal`-Auftrag: rein datengetrieben nach algorithmischen Mustern suchen, die noch nicht im Wiki dokumentiert sind, und backtesten (kein vorgegebenes ICT-Konzept, offene Exploration). Zwei neue Skripte: `algo/explore_patterns.py` (Wochentag-Effekt, Stunde des Tages-High/-Low, Range-/Richtungs-Autokorrelation, Rundzahl-Magnetismus; n≈34 Tage, 1m/5m) und `algo/backtest_daily_patterns.py` (dieselben Fragen ausser "Stunde des Extrems" auf 1d-Baren, n=147, 2026-01-02 bis 2026-08-04 — 1d hat bei yfinance kein Lookback-Limit, `raw/marktdaten/` deckt dank vorherigem 1d/1h-Nachladen bereits das ganze Jahr ab). **Wichtiger Befund zur Methodik selbst**: die beiden Stichproben widersprechen sich teils (kleine Stichprobe: Montag 50% bullish/Mittwoch groesste Range, Richtungs-Reversion nach bullishen Tagen; grosse Stichprobe: Montag 78,6% bullish + groesste Range, leichtes Momentum) — n=147 gilt als massgeblich, n=34 war zu klein fuer Wochentags-/Autokorrelations-Aussagen. **Ergebnisse (n=147)**: (1) Montag sticht heraus — Median-Range 551,00 Pkt. (hoechste aller Wochentage) und 78,6% bullish (n=28) gegen 46-53% an den anderen Tagen; ist NICHT dasselbe wie die bestehende 70%-Wednesday-Regel ([[One Shot One Kill Model]]) oder "Wochenhoch/-tief bevorzugt Montag" ([[Market Maker Manipulation Templates]]) — beide beschreiben Timing der Wochenextreme, nicht Montags eigene Richtungstendenz. (2) Range-Autokorrelation r=0,305 (n=146) — echtes Volatility Clustering, grosse Tage folgen eher auf grosse Tage. (3) Richtungs-Autokorrelation schwaecher: nach bullishem Tag 58,8% bullish am naechsten Tag (n=80, leichtes Momentum), nach bearishem 51,5% (n=66, quasi Zufall). (4) Rundzahl-Magnetismus: kein Effekt, Ø-Abstand zur naechsten 50er-Marke 12,25 Pkt. gegen 12,5 Pkt. Erwartungswert bei Gleichverteilung (n=294) — stabiles Nullresultat ueber beide Stichproben. Punkte 1+2 sind robuste Kandidaten fuer eigene Konzept-Seiten, falls sie sich mit mehr Daten halten; Seite `wiki/synthesis/Statistische Muster jenseits der ICT-Konzepte (Backtest).md` angelegt. |
| 2026-08-04 | Nutzerwunsch: Seite auf "(laufend)" umbenannt (aktualisiert bei neuen Daten statt statischer Schnappschuss), plus neue Standardregel: staendig gegenpruefen + Vorschlaege machen, unsichere Nutzerthesen als offene Hypothese festhalten, falsifizierte Funde loeschen statt nur markieren (siehe Merkzettel). Gegenprobe fuer den Montags-Befund direkt nachgeliefert: Monats-Aufschluesselung zeigt den Montags-Vorsprung in 5/7 Monaten (Jan/Feb/Mär/Jun/Jul), aber **nicht im Mai** (33% vs. 76%, dort war Montag schwaecher) und ohne klaren Vorsprung im April (beide Seiten stark bullish). Kein Ein-Monats-Artefakt, aber auch kein Automatismus — auf der Wiki-Seite ergaenzt. |
| 2026-08-04 | Nutzerwunsch: Muster auf Wochen-/Monatsebene pruefen, dafuer auch externe Seasonal-Tendency-Quellen aus dem Netz nutzen. Web-Recherche (Equity Clock, Barchart, Quantpedia, ETF Trends, Forbes, Deephaven — Links auf der Wiki-Seite) zu drei Themen: Nasdaq-100-Monats-Seasonality (20-Jahres-Basis: beste Monate Jan/Mär/Apr/Mai/Jul/Aug/Okt/Nov), Turn-of-Month-Effekt (extern sehr gut belegt, 19+ Laender), "Sell in May" (gilt fuer Tech/Nasdaq laut mehreren Quellen deutlich schwaecher als S&P 500). `algo/backtest_seasonal.py` neu: testet Turn-of-Month (letzter Handelstag + erste 3 des Folgemonats vs. Rest) und Woche-im-Monat direkt an den eigenen 147 1d-Baren, plus Monatszahlen 2026 als Rohbefund gegen die externen Quellen gehalten (ausdruecklich **kein** echter Mehrjahres-Seasonality-Test, da nur 1 Jahr Historie). **Turn-of-Month bestaetigt sich in den eigenen Daten**: TOM-Fenster (n=28) Ø-Tagesrendite +0,341%/64,3% bullish gegen Rest-Monat (n=221) +0,070%/52,5% — bislang der einzige Fund mit UND externer UND eigener Bestaetigung. Woche-im-Monat: Woche 1+2 ueberschneiden sich mit TOM (kein unabhaengiger Fund), aber Woche 4 faellt mit 44,1% bullish (n=34) unerwartet ab — neuer, noch unbestaetigter Kandidat fuers naechste Update. Monatsvergleich gegen externe 20-Jahres-Seasonality: April/Mai decken sich (beide "beste Monate" laut Quellen, beide 2026 stark bullish, Mai bestaetigt auch die "Sell-in-May gilt fuer Tech kaum"-Beobachtung), **Maerz/Juli widersprechen klar** (beide historisch "beste Monate", in 2026 aber die schwaechsten, 36,4% bullish). Alles auf `wiki/synthesis/Statistische Muster jenseits der ICT-Konzepte (laufend).md` ergaenzt, inkl. Quellenlinks. |
| 2026-08-04 | Nutzerwunsch: eigene "Seasonal Tendency" aus den eigenen Daten bauen, als Gegenpruefung zur ICT-Quellenseite UND als spaetere Datenbank. `algo/backtest_seasonal.py` erweitert: Wochentag-Tabelle ergaenzt (fehlte bisher hier, stand nur in `backtest_daily_patterns.py`), schreibt jetzt zusaetzlich `algo/seasonal_tendency.json` (Wochentag/Monat/Turn-of-Month/Woche-im-Monat je mit n, Bullish%, Ø-Rendite, Median-/Ø-Range) -- versioniert (kein `.gitignore`-Treffer, anders als `algo/live/*/`), gedacht um naechstes Jahr Jahr-1 gegen Jahr-2 zu vergleichen statt bei jeder Frage neu zu rechnen. Neue Seite `wiki/synthesis/Seasonal Tendency (Eigene Daten, laufend).md` als dediziertes Gegenstueck zu `wiki/concepts/Seasonal Tendency.md` (ICT-Lehrmeinung ohne Zahlen) -- buendelt alle kalendarischen Funde an einem Ort statt sie ueber zwei Seiten zu verteilen. Kalendarische Abschnitte (Wochentag, Turn-of-Month, Woche-im-Monat, Monat) aus der aelteren "Statistische Muster..."-Seite dorthin verschoben (nicht dupliziert), die verbleibt fuer nicht-kalendarische Funde. Bonus-Zahl beim Wochentag-Ausbau entdeckt: Montags Ø-Tagesrendite +0,71% -- mehr als das Fuenffache jedes anderen Wochentags, staerkt den bereits bekannten Montags-Befund zusaetzlich. |
| 2026-08-04 | Nutzerfrage "was sind deine Gedanken, erkennst du weitere Muster?" -- proaktive Gegenpruefung + neue These, unaufgefordert. (1) Gegenprobe: ist der Montags-Effekt nur ein Turn-of-Month-Artefakt (beide bullish, koennten sich ueberschneiden)? Nein: nur 7/28 Montage liegen im TOM-Fenster, die uebrigen 21 "reinen" Montage liegen bei 76,2% bullish/+0,70% Rendite -- praktisch identisch zu den TOM-Montagen (85,7%/+0,73%). Montags-Effekt haelt unabhaengig von TOM. (2) Neuer Detektor `ndog_gap()` in `tools/analyze_ohlc.py` (Backlog-Posten NWOG/NDOG erstmals umgesetzt) -- Gap zwischen Vortages-Close und Tages-Open an der Mitternachtsgrenze, anders als das 16:14-verankerte ORG. Wichtiger Stolperstein: 1d-CSV-Dateien haben pro Datei nur eine Kerze mit Open-Zeit 18:00 des VORABENDS (Globex-Konvention) -- `bar.t.date()` zeigt auf den Vortag, nicht den Handelstag selbst; ndog_gap()s datumsbasierte Logik funktioniert deshalb nur fuer echte Intraday-Mehrkerzen-Tage (z.B. live_status.py), nicht fuer die 1d-Einzelkerze. `algo/backtest_ndog.py` rechnet stattdessen direkt auf der Bar-Reihenfolge wie `backtest_seasonal.py`. Ergebnis (n=146): Korrelation \|Gap\| vs. Tagesrange r=0,264; Fill-Quote 86,3% insgesamt, aber 98,6% bei kleinen vs. nur 74,0% bei grossen Gaps -- bestaetigt quantitativ die bereits dokumentierte ORG-Regel "Gap-Groesse entscheidet ueber den Fill", hier erstmals fuer NDOG beziffert; Gap-Richtung setzt sich nur zu 43,2% fort (leichte Fade-Tendenz). `algo/live_status.py` liefert jetzt zusaetzlich ein `ndog`-Feld (analog `org_ce`), `/algo-live-status` berichtet es -- damit ist die alte Nutzer-Vorgabe "NDOG/NWOG immer mit Opening/Closing-Preisen hinterlegen" technisch umgesetzt statt nur vermerkt. |
| 2026-08-04 | NWOG-Pendant nachgezogen: `nwog_gap()` in `tools/analyze_ohlc.py` (duenner Wrapper um `ndog_gap()`, nur montags aktiv, sonst `None`). `algo/backtest_nwog.py` testet direkt die NWOG-Bias-Regel und beide Timing-Behauptungen aus `wiki/concepts/New Week Opening Gap (NWOG) Bias.md`. Ergebnis (n=28 Wochen): **Bias-intakt-Quote nur 7,1%** (2/28) -- das NWOG wird in fast allen Wochen irgendwann wieder erreicht, selbst ab Dienstag gerechnet (Montags-Kerze ausgeklammert) nur 10,7%. Korrelation \|Gap\| vs. Wochenrange r=0,124 (deutlich schwaecher als NDOGs 0,264). Gap-Richtung vs. Wochenrichtung exakt 50,0% -- reiner Zufall, kein Fade/Momentum wie bei NDOG. Wochentag-Verteilung: Wochen-Low bevorzugt tatsaechlich Montag (32,1%, hoechster Wert) -- teilweise bestaetigt; Wochen-High dagegen NICHT (nur 14,3%, hinter Di/Mi mit je 28,6%); Donnerstag-Reversal-These klar widerlegt (Donnerstag unwahrscheinlichster Tag fuer beide Extreme, 7,1%/0,0%). `algo/live_status.py` liefert montags zusaetzlich ein `nwog`-Feld, `.claude/commands/algo-live-status.md` aktualisiert. NDOG/NWOG-Vorgabe damit vollstaendig umgesetzt. |
| 2026-08-05 | **Neue Standardregel**: Walk-Forward-Test, Monte-Carlo-Simulation und Parameter-Sensitivitaet werden ab jetzt Standardwerkzeuge fuer jeden Regel-Backtest (nicht nur die einmalige `bt.run()`-Zahl aus `backtest_bt.py`). `algo/backtest_walkforward.py` neu (fuer `SilverBulletStrategy`/`rules.py::plan_trade`): (1) Parameter-Sensitivitaet rastert `stop_buffer_pct` (0,05/0,10/0,20/0,30/0,50); (2) Walk-Forward in 6 rollierenden Folds (~7 Handelstage je Fold, In-Sample-Grid-Search -> Out-of-Sample ohne Refit); (3) Monte Carlo resampled die Baseline-Trade-Returns 1000x. Datengrundlage: 47 Handelstage (2026-06-08 bis 2026-08-04, 5m) — das ist bereits das yfinance-Limit fuer 5m-Historie (~60 Tage), mehr Intraday-Historie ohne zweite Datenquelle nicht verfuegbar; bei n=97 Trades insgesamt (noch weniger je Fold) sind alle drei Analysen Groessenordnungs-Schaetzungen, keine belastbaren Ergebnisse. **Ergebnis, alle drei Verfahren stuetzen sich gegenseitig**: (1) Parameter-Sensitivitaet — Profit Factor bleibt bei jedem getesteten Stop-Puffer < 1 (0,05->0,470, 0,10->0,520, 0,20->0,493, 0,30->0,657, 0,50->0,761), Expectancy durchgehend negativ (-0,041% bis -0,020%/Trade) — groesserer Puffer mildert den Verlust, macht die Basisregel aber nicht profitabel. (2) Walk-Forward — kein Fold erreicht Out-of-Sample Profit Factor > 1 (0,506/0,000/0,425/0,204/0,887 ueber 5 Folds), alle 76 OOS-Trades zusammen kumulierte Rendite -3,50%. (3) Monte Carlo (Baseline stop_buffer_pct=0,10, 1000 Resamples) — selbst das 95.-Perzentil der kumulierten Rendite liegt bei -0,8% (nie profitabel), Median -3,7%. **Schlussfolgerung**: die Basisregel aus `rules.py` (Silver-Bullet-Fenster + FVG-Trigger ohne Confluenz) hat auf dieser Stichprobe robust negative Erwartung — kein Artefakt von Trade-Reihenfolge, Stop-Puffer-Wahl oder Zeitfenster-Auswahl. Bestaetigt/verschaerft den ersten `backtest_bt.py`-Befund (53 Trades, WR 15%, PF 0,32), siehe Backlog-Punkt 2 oben. `algo/fetch_yfinance.py` fuer 05.08.2026 nachgezogen (Handelstag zum Laufzeitpunkt noch nicht beendet); zweiter Lauf mit diesem zusaetzlichen Tag (48 Handelstage, n=99 Trades) bestaetigt: Parameter-Sensitivitaet weiterhin PF < 1 bei jedem Puffer (0,46-0,75), Walk-Forward OOS zusammen -2,79% (n=73), Monte-Carlo-95.-Perzentil -1,1% — Groessenordnung unveraendert trotz eines zusaetzlichen Tages. |
| 2026-08-05 | Nachtrag zu Task 7 (RenTec-Ensemble-Plan): `validate.py`s sieben Einzel-Format-Parameter (`title`/`col_label`/`col_width`/`value_fmt`/`is_col_label`/`is_col_width`/`is_value_fmt`/`header_prefix`) zu einem optionalen `fmt`-Dict konsolidiert, `backtest_walkforward.py` entsprechend angepasst. **Regressionscheck real ausgefuehrt** (nicht nur behauptet): `python algo/backtest_walkforward.py MNQ` vor und nach dem Refactor komplett durchlaufen lassen (je ~23 Min, 1237 Zeilen Ausgabe), dann Byte-Vergleich. Rohdiff zeigte zunaechst Abweichungen, alle auf tqdm-Fortschrittsbalken-Rauschen zurueckgefuehrt (`Backtest.run: NN%\|...bar/s]`-Fragmente plus ein Race zwischen tqdm-Flush und `warnings.warn()` bei der SL/TP-"dubious"-Warnung, das mal ein, mal kein fuehrendes Leerzeichen vor der Warnzeile hinterlaesst) -- nach Normalisieren dieser beiden Muster sind beide Laeufe SHA256-identisch. Commit `01d49cc`. `algo/seasonal_tendency.json` hatte parallel einen unabhaengigen Diff (n_days 147->148, neuer Handelstag 05.08. durch einen fruehreren `backtest_seasonal.py`-Lauf) -- bewusst NICHT mit in diesen Commit gepackt, da inhaltlich getrennt (Daten-Refresh statt Code-Fix); bleibt als offene Aenderung im Arbeitsverzeichnis. |
| 2026-08-04 | Nutzerwunsch: TGIF-Setup fuer Freitag mitberechnen. `algo/backtest_tgif.py` neu (nutzt `group_weeks()` aus `backtest_nwog.py`): prueft die 20-30%-Weekly-Range-Retracement-These aus `wiki/concepts/TGIF (Thank God its Friday).md`. Operationalisierung mangels exakter Formel in der Quelle: Wochenrichtung ueber Montag-Open vs. Close des vorletzten Handelstags (nicht hart "Donnerstag", damit feiertagsverkuerzte Wochen mitzaehlen), Retracement = Abstand Freitag-Close vom relevanten Wochenextrem in % der Wochenrange. Ergebnis (n=27): **exakte Trefferquote nur 3,7%** (1/27), auch mit groszuegigerer Toleranz (15-35%) unveraendert. **Median 22,1%** trifft die Zielzone fast exakt -- aber Verteilung ist bimodal, kein Cluster um 20-30%: 37% der Wochen retracen kaum (0-10%), 48% deutlich mehr (50-100%). Median ist hier ein Split-Artefakt zwischen "kaum" und "viel", kein echtes Clustering-Signal. Auf `wiki/synthesis/Statistische Muster jenseits der ICT-Konzepte (laufend).md` (Abschnitt 6) und `wiki/concepts/TGIF (Thank God its Friday).md` dokumentiert. |
| 2026-08-06 | **Praezisions-Audit abgeschlossen** (Spec: docs/superpowers/specs/2026-08-06-algo-backtest-precision-audit-design.md, Plan: docs/superpowers/plans/2026-08-06-algo-backtest-precision-audit.md). Neu: algo/pnl.py (echter $-P&L nach Punktwert MNQ=$2/NQ=$20/ES=$50, konservative Mehrdeutigkeits-Aufloesung, Risiko-Sizing), algo/selfcheck.py, algo/README.md. Zwei echte Bugs gefixt: (1) backtest_ensemble.py::_risk_size vergass den Punktwert-Faktor -- reales Risiko war doppelt so hoch wie die beabsichtigten 1%; (2) backtest_bt.py::SilverBulletStrategy hatte GAR keine Risiko-Begrenzung (Lib-Default ~100% Equity-Notional statt 1%-Regel). dubious_pct (Anteil Trades mit Stop/Ziel in derselben Kerze) ist jetzt Pflichtzeile in jedem Report (validate.py, stress_test.py, backtest_bt.py). Bewusst nicht gefixt: stress_test.py's Tages-Fallback-Sizing (dokumentierte Grenze, kein $-Betrag ausgewiesen). Lookahead-Audit der 11 exploratorischen Skripte: alle 11 bestanden, dabei aber ein Zusatzfund (ausserhalb Scope, nicht gefixt): `backtest_seasonal.py::turn_of_month()` zaehlt interne Kalendertage der `rest`-Gruppe doppelt (ueberlappende `rs[:-1]`/`nrs[3:]`-Slices, keine Set-Deduplizierung) -- `rest.n` faelschlich ~221 statt ~120; reine Stichprobengroessen-Verzerrung, kein Lookahead, `signals.py` liest nur `tom["window"]` und ist unbetroffen -- Kandidat fuer einen eigenen Folge-Task. Security-Scan: keine Funde. Teilprojekt B (Wiki-Strategie-Findung, taegliche Erinnerung) folgt als eigener Spec. |
| 2026-08-07 | **Nachtrag zum Praezisions-Audit: finale Whole-Branch-Review fand 2 weitere Critical-Bugs, beide gefixt.** Eine abschliessende Review (Modell opus) ueber den gesamten Audit-Diff hat zwei Fehler gefunden, die in den einzelnen Task-Reviews durchgerutscht waren, weil dort nur die jeweilige Spezifikation geprueft wurde, nicht ein End-to-End-Lauf gegen echte Daten samt Margin-/Commission-Abgleich: (1) `algo/pnl.py::risk_size()` hatte keine Margin-Obergrenze -- `backtest_bt.py` verwarf dadurch 60 von 99 moeglichen Setups durch stille Broker-Margin-Cancellations, systematisch verzerrt Richtung eng gestoppter (und damit kontraktreicher) Setups; propagierte in `backtest_walkforward.py`s Parameter-Sensitivitaets-Tabelle (verglich dadurch unterschiedliche Trade-Populationen statt nur den Stop-Puffer). (2) `real_pnl()` ignorierte die Commission-Spalte komplett -- die als "Echte $-P&L" beworbene Zahl lag um den Faktor ~12 daneben (-1.136 USD statt der tatsaechlichen ~-13.830 USD laut Equity Final). Fix: `risk_size()` bekam einen optionalen `max_notional`-Parameter (Margin-Deckel), an EINER Stelle implementiert und in `SilverBulletStrategy` (neu: `leverage=20`-Attribut) UND `EnsembleStrategy` (ersetzt deren bisher duplizierte Inline-Kappung) verdrahtet; `real_pnl()` zieht jetzt `Commission` ab (Docstring: NETTO-Ergebnis). Zusaetzlich gefixt: `backtest_bt.py`s Symbol war fuer Punktwert/$-Anzeige hart auf "MNQ" verdrahtet (waere bei anderem Symbol 25x falsches Risiko gewesen), README.md praezisiert (die Preiskorrektur `flag_dubious` laeuft nur in `backtest_bt.py`, nicht in `validate.py`/`stress_test.py`, die nur `dubious_pct` als Prozentzahl melden). **Ergebnis nach dem Fix ist deutlich schlechter, nicht besser**: `python algo/backtest_bt.py MNQ` liefert jetzt 98 statt 39 Trades, Equity Final -43,06% (vorher unbemerkt bei -13,8% durch die verworfenen Trades), Commissions $46.065 -- die Strategie handelt bei 20x Hebel und engen Silver-Bullet-Stops praktisch durchgehend am Margin-Limit, das war vorher durch die stillen Cancellations verdeckt. Der 2026-08-05-Walk-Forward-Log-Eintrag (PF 0,32-0,89 je nach Puffer/Fold) basiert auf der Vor-Fix-Population und ist damit **veraltet, ein Re-Lauf steht noch aus**. Ein Rest-Problem blieb bewusst offen (nicht gefixt, dokumentiert): 1 von 102 Order-Platzierungen wird weiterhin storniert, Ursache diesmal eine andere (die `backtesting`-Lib laesst Limit-Orders nie verfallen; die stornierte Order wurde Wochen vor ihrem Fill platziert, waere nach `rules.py`s striktem 1-Stunden-Fenster ohnehin kein gueltiger Trade) -- echter Fix waere Order-Verfall am Fensterende in beiden Strategien, als eigener Folge-Schritt vorgemerkt. Ebenfalls offen: `commission=0.0002` ist Notional-proportional (Aktien-Modell) statt $/Kontrakt (Futures-Realitaet) und erzeugt bei 20x Hebel die $46k Gebuehren -- sollte vor jeder Kapitalentscheidung auf ein realistisches Futures-Kommissionsmodell umgestellt werden. Vollstaendiger Fund-/Fix-Bericht (inkl. aller Zahlen und Verifikationslaeufe) lag waehrend der Session unter `.superpowers/sdd/2026-08-06-algo-backtest-precision-audit/` (git-ignoriertes SDD-Scratch-Verzeichnis, siehe Commits `6ecbc55..7a4b403` fuer den vollstaendigen Code-Diff). |
| 2026-08-07 | **Code-Review des Praezisions-Audits (Diff bd29d0b..966924f) per Subagent, 2 Important-Funde, beide gefixt.** Review fand keine Critical-Bugs, aber zwei echte Luecken: (1) `pnl.py::flag_dubious()` liess entgegen der urspruenglichen Design-Signatur (`docs/superpowers/specs/2026-08-06-algo-backtest-precision-audit-design.md:45`) den `bars`-Parameter weg und korrigierte JEDEN Same-Bar-Trade pauschal auf den Stop-Preis -- gegen die `backtesting`-Lib-Quelle verifiziert (`backtesting.py::_process_orders`, Kommentar "not ambiguous, because stop and TP go in the same price direction"): liegt nur das TP im Bar-Range, das SL aber nie, war die Reihenfolge eindeutig, ein echter Gewinn-Trade wurde faelschlich zum Verlust umgewertet. Fix: `flag_dubious(trades, bars)` prueft jetzt per Entry-Kerzen-High/Low, ob SL UND TP beide erreichbar waren, korrigiert nur dann. Gemessener Impact auf dem aktuellen MNQ-Lauf: 0% (0 von 108 Trades betroffen, `dubious_pct` unveraendert bei 0,0%, Netto-P&L unveraendert -37.950,21 USD) -- der Bug war real, aber bei dieser Stichprobe folgenlos; potenziell relevant bei kuenftigen dubious-lastigen Konfigurationen (`backtest_walkforward.py` hatte laut 2026-08-06-Log "einen erheblichen Teil" dubious). (2) `validate_ensemble.py` setzte `EnsembleStrategy.point_value` nie auf das CLI-Symbol -- blieb beim Klassen-Default $2/Punkt (MNQ), `python algo/validate_ensemble.py ES` haette mit 25x zu kleinem Risiko-Sizing gehandelt (derselbe Bug-Typ wie der 2026-08-06-Fund in `backtest_ensemble.py`, hier beim Audit uebersehen). Fix: analoge Zeile wie in `backtest_bt.py` ergaenzt. `algo/selfcheck.py` (4/4 gruen) und `python algo/backtest_bt.py MNQ` real ausgefuehrt zur Verifikation, `pnl.py::demo()` um einen Testfall fuer den "nicht ambig"-Pfad erweitert. Zwei Minor-Funde (roher `KeyError` statt `ValueError` bei unbekanntem CLI-Symbol in `backtest_bt.py`; `leverage`/`margin`-Kopplung ohne Assertion in 3 Dateien) bewusst nicht gefixt -- kosmetisch, kein $-Impact. |
| 2026-08-07 | **Backtest-Skript-Entduplizierung & Audit (Phase 1 des TUI-Vorhabens, siehe Spec/Plan-Doku unten).** Nutzerwunsch: pruefen, ob die ~20 Einzeldateien in `algo/` Sinn ergeben oder in eine interaktive Oberflaeche gehoeren -- Entscheidung: Dateien bleiben getrennt (Git-Historie pro These), stattdessen Korrektheits-Audit + Entduplizierung als Fundament fuer eine spaetere TUI (Phase 2, eigener Spec). Neu: `algo/backtest_common.py` buendelt `find_1d_days()`/`load_rows()`/`pearson()` (vorher: `pearson()` 4x dupliziert in `backtest_ndog.py`/`backtest_nwog.py`/`backtest_daily_patterns.py`/`explore_patterns.py`) + `write_result()`. Alle 11 exploratorischen Skripte bekamen eine `run()`/`main()`-Trennung (Konsolenausgabe Diff-verifiziert unveraendert), jedes schreibt sein Ergebnis nach `algo/results/<name>.json` -- damit ist der seit 2026-08-03 offene PLAN.md-Backlog-Punkt 4 ("Backtest-Ergebnisse als Datenartefakt") erledigt. **Ein echter Bug gefunden und gefixt**: `backtest_seasonal.py::turn_of_month()` zaehlte Tage an Monatsuebergaengen doppelt (ueberlappende `rs[:-1]`/`nrs[3:]`-Slices akkumulierten `rest` inkrementell statt es als Komplement von `tom_days` zu berechnen) -- seit dem Praezisions-Audit-Nachtrag am 2026-08-06 dokumentiert, aber nie tatsaechlich behoben (ein bereits vorhandener `tom_days`-Filter taeuschte einen Fix vor, entfernte aber nur die TOM/rest-Ueberschneidung, nicht die rest/rest-Selbstdopplung). `wiki/synthesis/Seasonal Tendency (Eigene Daten, laufend).md` mit den korrigierten Zahlen aktualisiert. Zusaetzlich ein zweiter echter Bug gefunden und gefixt (ausserhalb des urspruenglichen Plan-Scopes, waehrend Task 8 entdeckt): `find_days()` in `algo/backtest_org_ce.py` filterte nicht nach Symbol, wodurch es bei Tagesordnern mit sowohl ES- als auch MNQ-Dateien (z.B. 23.07.2026) faktisch deterministisch zugunsten von ES auswaehlte (alphabetisch vor MNQ, kein `sorted()` auf die Kandidatenliste -- keine echte Zufaelligkeit, wie zunaechst hier dokumentiert): gemessen 40 von 45 betroffenen Tagen lieferten die ES- statt der MNQ-Datei, ~89 % der Tage rechneten also auf dem falschen Instrument -- der hartkodierte 2026-07-23-Regressionscheck crashte dadurch. Fix: `find_days(symbol="MNQ")` filtert jetzt korrekt (analog `backtest_ohlc.py`), betrifft/behebt implizit auch die vier Downstream-Konsumenten `backtest_fvg_specialness.py`/`backtest_midnight_range_std.py`/`backtest_midnight_range_judas.py`/`explore_patterns.py`. Sonst keine weiteren Bugs in den 11 Skripten gefunden (Audit-Kriterien: Doppelzaehlung, Off-by-one, Division-durch-Null, ehrliche Stichprobengroesse -- ueber den bestehenden 2026-08-06-Lookahead-Check hinaus). `algo/selfcheck.py` bekam einen neuen `dedup`-Check (ruft `run()` fuer alle 11 Skripte auf, prueft dict-Rueckgabe + gueltiges JSON, kein Zahlen-Assert). Phase 2 (interaktive TUI-Oberflaeche darauf aufbauend, "Bloomberg-Terminal"-Anspruch des Nutzers) ist ein eigener, spaeterer Spec. |
| 2026-08-08 | **Wiki-Ingest fuer die MCPT-Quelle nachgeholt** (Design stand bereits seit heute frueh in `docs/superpowers/specs/2026-08-08-algo-permutation-test-design.md`, dort explizit als offener Nebenschritt vermerkt). Nutzer legte `raw/literatur/How I Develop Trading Strategies  Permutation Tests and Trading Strategy Development with Python.md` an (inhaltsgleiches Scratch-Duplikat vorher schon unter `raw/md.md`) — kein ICT-Material, sondern generische Validierungsmethodik (Bar-Permutationstest / MCPT nach Timothy Masters). Neue Seiten: `wiki/sources/How I Develop Trading Strategies (Source).md`, `wiki/concepts/Monte Carlo Permutation Test (MCPT).md`, `wiki/concepts/Vier-Stufen-Strategieentwicklung (Masters).md` (In-Sample Excellence -> In-Sample MCPT -> Walk-Forward -> Walk-Forward MCPT). `algo/permutation_test.py` selbst ist weiterhin **nicht implementiert** — bleibt offener Backlog-Punkt, dieser Eintrag deckt nur den Wissens-/Wiki-Teil ab. |
| 2026-08-08 | **Masters' Buch "Testing and Tuning Market Trading Systems" ingestet** (Primaerquelle hinter den heute frueh aus einem YouTube-Transkript angelegten Seiten [[../wiki/concepts/Vier-Stufen-Strategieentwicklung (Masters)|Vier-Stufen-Strategieentwicklung]] und [[../wiki/concepts/Monte Carlo Permutation Test (MCPT)|MCPT]]). 12 neue Konzeptseiten, siehe `wiki/log.md`. Vier neue Backlog-Punkte (7-10) oben, alle mit direktem Bezug auf bestehenden `algo/`-Code: (1) Kennzahlen auf Bar- statt Trade-Basis rechnen — die `backtesting`-Lib liefert nur Trade-Renditen, und Masters zeigt, dass Profit Factor/Sharpe darauf systematisch extremer ausfallen (sein Beispiel: unendlich statt 1,01); (2) Guard Buffer in `validate.py` pruefen; (3) BCa-Untergrenzen statt reiner Punktschaetzer, und: das Monte-Carlo-Trade-Resampling in `validate.py` ist exakt Masters' als "incorrect" bezeichneter naiver Drawdown-Bootstrap, der bei kleiner Stichprobe Katastrophen-Drawdowns um Faktor 13,65 unterschaetzt — vor jeder Kapitalentscheidung relevant; (4) Return-Partitionierung (Skill/Trend/TrainingBias) und `unbiased_pval` als fast gratis mitlaufende Zusatzauswertungen im weiterhin nicht implementierten `algo/permutation_test.py`. Ebenfalls bestaetigt (kein Handlungsbedarf): dass `validate.py` auf Walk-Forward statt Cross Validation setzt, ist laut Buch richtig — CV leckt bei nichtstationaeren Marktdaten Zukunftsinformation. Kein Code geaendert, reiner Wissens-/Planungs-Eintrag. |
| 2026-08-08 | **`algo/masters.py` angelegt — Validierungs-Werkzeugkasten aus dem Masters-Buch, portiert und lauffaehig.** Nutzerfrage war, ob die Code-Beispiele des Buches lesbar und nutzbar sind. Befund: das Buch druckt seinen C++-Code nur in **Fragmenten**, die vollstaendigen .CPP-Dateien liegen NICHT im EPUB (Masters verweist auf seine Website). Die abgedruckten Fragmente decken die algorithmische Substanz aber vollstaendig ab, und alles, was nur referenziert wird (STATS.CPP mit t-/Beta-Verteilung, SVDCMP, QSORTD, GLOB_MAX, BRENTMAX, EVER_RS), hat exakte scipy/numpy-Entsprechungen. Portiert wurden: `guard_buffer`/`walkforward` (Guard Buffer + Varianz-Inflation), `entropy`/`clean_tails`/`gap_analyze` (Indikator-Vorpruefung), `drawdown`/`dd_to_pct`/`drawdown_quantiles`/`drawdown_bound`/`drawdown_bound_naive` (Doppel-Bootstrap vs. das billige Verfahren), `lower_bound_t`/`lower_bound_bca`/`profit_factor`/`log_profit_factor`/`sharpe_ratio`, `return_bound`/`orderstat_tail`/`quantile_conf`, `permute_prices`/`permute_bars`/`permute_multi`, `cscv`, `stoc_bias`, `partition_return` und als Bruecke zur bestehenden Infrastruktur `bar_returns_from_trades()` (rechnet `stats._trades` der `backtesting`-Lib in Bar-Renditen um -- damit ist Backlog-Punkt 7 werkzeugseitig erledigt, offen bleibt das Verdrahten in die Reports). **Bewusst nicht portiert:** Elastic Net (-> `sklearn.linear_model.ElasticNetCV`, Namensfalle: sklearns `alpha` = Masters' Lambda, `l1_ratio` = sein Alpha), Differential Evolution (-> `scipy.optimize.differential_evolution`), PARAMCOR (haengt an einer DE-Endpopulation). Der Selbstcheck prueft Eigenschaften statt nur Durchlauf und ist in `algo/selfcheck.py` eingehaengt (jetzt 7 Checks, alle gruen): permutierte Bars erfuellen alle vier Struktur-Bedingungen, die Multi-Markt-Permutation erhaelt die Korrelation (Abweichung < 0,02), CSCV trennt einen echten Edge (**0,00**) von reinem Rauschen (**0,86**), der Doppel-Bootstrap ist konservativer als der naive (24,98 % vs 19,28 % Drawdown, Faktor 1,34), StocBias zeigt an 400 reinen Rausch-Kandidaten IS **+0,2467** gegen OOS **-0,0298**, und die Identitaet aus Gleichung 7-2 geht exakt auf. `scipy>=1.10` in `algo/requirements.txt` nachgetragen (war undeklariert genutzt); `scikit-learn` war deklariert, aber in der Umgebung nicht installiert -- nachinstalliert, `selfcheck.py` lief vorher gar nicht durch. Alle 13 Masters-Wikiseiten haben jetzt einen Abschnitt "Implementierung" mit der zustaendigen Funktion bzw. der Begruendung, warum bewusst nicht portiert wurde. |
| 2026-08-10 | **Neue offene These, noch nicht operationalisiert**: Preis baut am Start eines Macro-Fensters (Anlass: 10:50 Uhr NY, [[NY Lunch Macro Model]]) sichtbar Kraft/Energie auf ("Spooling"), bevor der eigentliche Move einsetzt. Auf `wiki/concepts/ICT Macros & Leading Candles.md` als offene Hypothese dokumentiert statt sofort gebacktestet, da noch keine praezise Definition vorliegt (Kandidaten: engere Ranges + steigendes Volumen vor dem Fenster, mehrere Same-Direction-Closes in Folge, sinkender Wick-Anteil) — sobald der Nutzer eine der Operationalisierungen bestaetigt oder mit konkreten Chart-Beispielen praezisiert, `algo/backtest_macro_spooling.py` nach dem `find_days()`-Muster bauen. Gleichzeitig Standardverfahren erweitert: Qs/Os/Hs-Level-Tabelle + C.E-Respekt-Check gilt jetzt fuer alle ORGs (NY-AM/PM/Midnight-London/London/NY-Pre-Session), nicht nur das RTH-9:30-Gap. |
| 2026-08-10 | **Nutzerbeobachtung geprueft: "unnatuerlicher offener Bereich" MNQ 1m um 12:30/12:31 NY** -- Jannes vermutete einen TradingView-Anzeigefehler. Gegenpruefung gegen yfinance (unabhaengige Quelle) zeigt die Luecke identisch: **kein Anzeigefehler, echtes Preisvakuum**. Zeitlich liegt es zwischen der 12:31- und der 12:32-Bar (Bar-Label = Bar-Start), nicht 12:30/12:31 -- Jannes' Augenmass war eine Minute frueh. 12:31 Low 29.806,25 -> 12:32 High 29.787,25 = **19,00 Punkte**, in denen kein einziger Print liegt; Open-to-Close-Sprung 29.814,25 -> 29.783,50 = 30,75 Punkte. Volumen der 12:32-Bar 10.720 gegen Median 3.370 (11:00-12:31) = **3,2x** -- Impuls, kein duenner Feed. Keine fehlende Minute im ganzen RTH-Fenster 09:30-13:30 (0 Luecken), also auch kein Bar-Ausfall. Vakuum um **13:10 NY komplett durchgehandelt** (38 min offen). Neu gebaut: `algo/backtest_1m_gaps.py` (reuse von `tools.analyze_ohlc.load/find_files`, `--selfcheck` mit 4 Asserts) -- zaehlt Vakuen nur zwischen echt benachbarten Minuten (t2-t1 == 60s), damit Session-Pausen nicht als Gap durchgehen. Ergebnis ueber `raw/marktdaten/`: MNQ 23 Tage / 28.839 Minutenpaare -> nur **16 Vakuen ueberhaupt (0,055 %)**, davon genau **eines >= 10 Pkt** (20,75 Pkt am 2026-08-04 16:15, also nach RTH-Close). ES 18 Tage: 139 Vakuen, ebenfalls nur eines >= 10 Pkt (20,50 Pkt am 2026-07-28 01:19, Asia-Session). **Jannes' Einschaetzung "so noch nie gesehen" ist quantitativ bestaetigt**: ein Vakuum dieser Groesse kommt ca. alle 23 Handelstage vor, und die beiden Vergleichsfaelle liegen beide in duennen Randzeiten -- der heutige ist der erste im vorliegenden Datenbestand mitten in der liquiden Lunch-Phase. Offen fuer morgen: Ausloeser (News/Block-Order) unbekannt, nicht recherchiert. Erinnerung an Jannes fuer den Bias am 2026-08-11 als Memory hinterlegt. |
| 2026-08-10 | **Macro-Fenster gebacktestet (`algo/backtest_macro.py` neu) — Anlass: Jannes markiert im Chart das Macro 09:50-10:10 (MNQ, 2026-08-10).** Aufbau: jeder Tag in 72 lueckenlose 20min-Bloecke, pro Stunde `:50-:10` (Macro) gegen `:10-:30` und `:30-:50` (Kontrolle) — die Kontrollen liegen direkt daneben, damit der Tageszeit-Confounder raus ist (sonst gewaenne 09:50-10:10 allein wegen der Naehe zum RTH-Open). Reuse: `tools.analyze_ohlc.load/at`, `backtest_common.write_result`; `--selfcheck` mit 5 Asserts (72 Bloecke, 24 Macros, lueckenlos, Range/Netto-Rechnung, MIN_BARS-Verwurf). Basis MNQ 23 Tage / 1091 Bloecke. **Ergebnis: Macros sind real, aber anders als erwartet** — median Range nur **+9 %** (63,50 vs 58,25), median Netto dagegen **+32 %** (31,50 vs 23,88), median dir 0,52 vs 0,46; Mann-Whitney einseitig Range p=0,0028, Netto p<0,0001, dir p=0,0003. Also nicht *mehr* Volatilitaet, sondern *gerichtetere*. **Gegen die These**: 09:50-10:10 ist nicht das groesste Fenster — Median-Rang 3 von 49, geschlagen vom RTH-Open-Block 09:30-09:50 (Rang 2, medRange 199,12 vs 152,38). Vorbehalt: 23 Tage, Bloecke desselben Tages nicht unabhaengig -> p optimistisch. **Nebenbefund, der einen Wiki-Eintrag von heute frueh korrigiert**: die 10:50-Spooling-Notiz behauptete, Preis verbringe die ganze 10:50-11:10-Macro-Zeit komprimiert und expandiere erst danach. Die 1m-Daten sagen das Gegenteil: 10:50-11:10 war der staerkste gerichtete Block des Tages (netto **-87,25** bei 106,50 Range, dir **0,82**), die Kompression lag 11:10-11:50 (dir 0,08 / 0,24), die Aufloesung Richtung NWOG 33 im *naechsten* Macro 11:50-12:10 (+30,25, dir 0,46). Fensterlage per Auge um ~20 min verschoben — derselbe Fehlertyp wie beim 1m-Vakuum am selben Tag (dort eine Minute). Spooling-Hypothese bleibt bestehen, beschreibt aber die **Zwischenphase** zwischen zwei Macros, nicht den Macro-Start; `algo/backtest_macro_spooling.py` entsprechend umdefinieren, wenn es gebaut wird. Heutige Tagesdaten liegen **nicht** in `raw/marktdaten/` (Handelstag lief noch, Teiltag darf nicht ins Archiv) — Verifikation lief gegen einen separaten yfinance-Zug, Datenstand bis 13:25 NY. |
| 2026-08-10 | **PM-Silver-Bullet-Macro 13:50-14:10 als Gegenbeispiel zum Macro-Befund von heute frueh** (Anlass: Jannes' Tape-Reading, kein Trade). Historischer Median dieses Blocks aus `algo/backtest_macro.py` (22 Tage): Range 56,38, Netto 35,12, dir 0,59 — einer der gerichtetsten Bloecke des Nachmittags. **Heute**: Range 23,75, Netto **-2,25**, dir **0,09**. Der Block lieferte also rund ein Zwanzigstel seines ueblichen Nettowegs. Damit ist Jannes' Absage ("zu wenig Punkte, gerade mal 30 ab hier") nicht nur relativ zu seinem Ziel richtig, sondern auch relativ zu dem, was dieses Fenster normalerweise hergibt. Bestaetigt zugleich den Kernbefund von heute frueh aus der Gegenrichtung: der Macro-Vorteil liegt im **Netto/dir**, nicht in der Range — und er ist ein Median, kein Versprechen. **Datenvorbehalt**: yfinance lag beim Abruf bei der 14:02-Bar, das Fenster ist mit 13 von 20 Minuten unvollstaendig; Zahlen mit dem TradingView-Export des Tages nachziehen. Ausserdem: Jannes' Chart (MNQU2026) und yfinance (MNQ=F, verketteter Front-Month) weichen im PM um einige Punkte ab — fuer Journal-Level gelten seine Chartwerte, fuer die Blockstatistik die Archivdaten. |
| 2026-08-10 | **These "das SB FVG soll im Macro entstehen" geprueft** (Anlass: Jannes' SB FVG/SIBI um 14:12 NY, lila markiert). `algo/backtest_macro.py` um FVG-Zaehlung je 20min-Block erweitert (reuse `tools.analyze_ohlc.fvgs`, neue CLI-Option `--min-fvg`). Ergebnis MNQ 23 Tage: FVGs haeufen sich in Macros, **und der Effekt waechst mit der FVG-Groesse** — >=2 Pkt: 2,82 vs 2,59 je Block (+9 %, p=0,0034); >=5 Pkt: 1,71 vs 1,51 (+13 %); **>=10 Pkt: 0,79 vs 0,62 (+27 %, p=0,0001)**; >=15 Pkt: 0,36 vs 0,30 (+20 %, p=0,0225, duenne Basis). **Praktisch wichtiger als der Vorsprung ist die Grundhaeufigkeit**: 96 % aller Macro-Bloecke enthalten mindestens ein FVG >=2 Pkt, im Schnitt fast drei pro 20 Minuten — "ein FVG im Macro" selektiert also nichts. Erst >=10 Pkt wird es selten genug (53 % der Macros gegen 40 % der Kontrollbloecke). Konsequenz fuer eine spaetere `rules.py`-SB-Regel: FVG-Mindestgroesse als Pflichtparameter fuehren, nicht FVG-Existenz. **Zeitlage-Befund**: das FVG entstand 14:12, also zwei Minuten NACH dem von ihm selbst markierten Macro 13:50-14:10 — innerhalb des SB-Fensters 14:00-15:00 aber schon. Welche der beiden Definitionen fuer "im Macro" gilt, ist offen und muss vor einer Kodierung entschieden werden. **Nicht verifiziert**: yfinance hing beim Abruf auf der 14:08-Bar (~9 min hinter seiner Chart-Zeit 14:17), die FVG-Level ~29.769,50 / ~29.760,00 (C.E. ~29.764,75) sind aus seinem Chart abgelesen, nicht aus Daten bestaetigt — mit dem TradingView-Export nachziehen. Chart-Konvention neu dokumentiert: lila FVG-Flaeche = immer SB FVG. |
| 2026-08-10 | **Sessionfehler in `backtest_macro.py` gefunden (noch nicht behoben).** `blocks()` startet bei 00:10 des **Kalendertags**, der MNQ-Handelstag beginnt aber 18:00 am Vorabend — die 1m-Exporte enthalten ihn korrekt (`MNQ 2026-07-09 1m.csv` = 2026-07-08 18:00 .. 2026-07-09 17:00). Dadurch fallen die Bloecke 18:00-24:00 heraus: **6 der 23 Macro-Fenster**, die gesamte Abend-/fruehe Asia-Session. Betroffen sind die publizierten Zahlen (351/740/1091 Bloecke, die drei p-Werte, "Median-Rang 3 von 49"). Richtung des Befunds (Macros = gerichteter, nicht groesser) bleibt, die Zahlen sind nach dem Fix neu zu erzeugen. Ausserdem: der Handelstag hat **23** Macro-Fenster, nicht 24 — 17:50 liegt in der Globex-Pause. Wiki-Seite mit Warnblock markiert. |
| 2026-08-10 | **Datenqualitaet `raw/marktdaten/` MNQ 1m vermessen** (Anlass: Design der Macro-Datenbank). (1) **Kein Volumen** in den TradingView-Exporten, nur `time,open,high,low,close` — jede volumenbasierte Spooling-Kennzahl ist damit unmoeglich. (2) **Systematische Exportluecke 23:59-00:08 an 15 von 19 vollen Tagen**, immer dieselben zehn Minuten am Datumswechsel; MNQ handelt dort durchgehend, also Artefakt statt Marktverhalten — macht Fenster 23:50-00:10 unbrauchbar (1 von 23 Tagen vollstaendig). Schliessbar per `fetch_yfinance.py` (MNQ=F 1m, ~30 Tage). (3) Vier Fragmenttage: 07-08, 08-03 (nur 11:19-16:18), 08-05, 08-07 -> **19 volle Tage**. (4) Bei strikter Vollstaendigkeit (20/20 Kerzen im Fenster + 10/10 Vorlauf): **440 von 529 moeglichen Fenstern (83 %)**, 18-22 Tage je Fenster; 16:50 faellt komplett aus (ragt ueber Sessionschluss 17:00). Nutzbar: **21 Fenster, ~440 Zeilen, +21 je Handelstag**. |
| 2026-08-10 | **Spec "Macro-Datenbank & Statistik-Skill"** geschrieben (`docs/superpowers/specs/2026-08-10-macro-datenbank-design.md`, Commit 94294c8c). Geplant: `algo/macro_db.py` (`build`/`stats`/`plot`) schreibt eine Zeile je Macro-Fenster (Vorgeschichte aus vorhandenen `analyze_ohlc`-Detektoren, Verlauf, `start_min` = Minute des Extrems entgegen der Netto-Richtung, getroffene Level) nach `algo/results/macro_db.csv`, plus Skill `.claude/skills/macro-db/` als Antwortdisziplin (Wilson-Intervall statt Punktschaetzung, Basisratenvergleich, unter n=20 keine Prozentzahl, Bonferroni-Hinweis bei Mehrfachvergleichen). Nutzerentscheidungen: nur echte MNQ-Daten (kein Fremd-Proxy), Spooling nicht vorab definieren sondern vier preisbasierte Kandidaten messen und die Daten entscheiden lassen, nur vollstaendig erfasste Fenster in die Statistik. **Nebenbefund**: Dukascopy liefert kostenlos Nasdaq-CFD-Tickdaten (`USATECHIDXUSD`, per Ladetest bis 2012 zurueck bestaetigt, ~3.500 Tage) — bewusst verworfen (CFD != Futures, Broker-Volumen wertlos, fruehe Jahre duenn), notiert falls die Stichprobe spaeter zum Engpass wird. |
| 2026-08-10 | **Alle Macro-Zahlen nach dem `blocks()`-Fix neu erzeugt + Selfcheck repariert.** Der Fix (Handelstag ab 18:00 Vorabend statt 00:10 Kalendertag) bringt die Abend-/Asia-Bloecke zurueck: **1417 statt 1091 Bloecke, Macro n=450 statt 351**. Der Befund wird dadurch **bestaetigt und geschaerft**, nicht gekippt: median Range 63,38 vs 57,75 (+10 %), median Netto 30,88 vs 24,50 (**+26 %**, vorher +32 %), dir 0,51 vs 0,46; alle p-Werte besser (Range 0,0004 / Netto 0,0001 / dir 0,0026 / fvgs 0,0054). FVG-Groesseneffekt **deutlicher** als vorher: >=10 Pkt jetzt 0,81 vs 0,61 = **+33 %** (vorher +27 %), >=15 Pkt 0,37 vs 0,28 = **+32 %** (vorher +20 %). Median-Rang von 09:50-10:10 unveraendert Platz 3, jetzt von 67 statt 49 Bloecken. **Bug im mitgelieferten Selfcheck behoben**: `assert labels[-1] == "16:30-16:50"` widersprach `assert len(bs) == 69` — 69 Bloecke ab 18:10 enden bei 16:50-17:10, der Selfcheck schlug also fehl. Auf "16:50-17:10" korrigiert, mit Kommentar: dieser letzte Block ragt 10 min ueber den 17:00-Close und wird von MIN_BARS immer verworfen (unvermeidbarer Randeffekt eines :10/:30/:50-Rasters auf einer 18:00-Session, kein Bug). |
| 2026-08-10 | **SB FVG vom 14:12 verifiziert** (vorher nur aus dem Chart abgelesen, yfinance hing auf 14:08). Nachzug um 14:34: 14:11 Low 29.767,25, 14:13 High 29.759,50 -> SIBI 29.759,50-29.767,25, **Groesse 7,75 Punkte**, C.E. 29.763,38. Preis danach bis 29.742,25 runter, bis 14:34 zurueck auf 29.764,75 (ueber die C.E. hinein), um 14:42 laut Chart 29.768,00 = Gap-Oberkante, also vollstaendig durchgehandelt. **Einordnung**: mit 7,75 Punkten liegt dieses SB FVG unter der 10-Punkte-Schwelle, ab der FVGs sich ueberhaupt im Macro haeufen — es gehoert zur haeufigen, nicht zur selektiven Sorte. Meine vorherige Pixel-Schaetzung (~29.769,50 / ~29.760,00) lag damit ~2 Punkte daneben. |
| 2026-08-10 | Ingest Core Content Month 09 (8 YouTube-Lektionen) liefert drei quantifizierte, backtestbare Regeln — als Backlog-Abschnitt "drei quantifizierte Regeln aus Core Content Month 09" oben notiert: London-Close-Retracement (mit zwei widerspruechlichen Parametersaetzen von ICT selbst, beide zu testen), 33-Pip-Protraction unter dem Midnight-Open (als Verhaeltnis zur ADR auf MNQ zu pruefen), Filling-The-Numbers-Vier-Level-Regel. Neu im Wiki: Flout-Range (15:00–00:00 NY, halbiert = Projektionseinheit) und die 5-Tage-ADR als eigenstaendiges Konzept. Noch kein Backtest gelaufen. |
| 2026-08-10 | Ingest der ICT-Gems-Macro-Reihe (6 von 11 Videos) korrigiert die **Messmethodik** von `algo/backtest_macro.py`: ICT sagt woertlich, der Move *beginne* im 20-Minuten-Fenster und laufe darueber hinaus — das Skript misst nur den Blockinhalt und unterschaetzt den Effekt dadurch systematisch. Zusaetzlich: die letzte Handelsstunde folgt einem eigenen Raster (15:15-15:45, 15:45-16:00) und wird derzeit falsch gerastert. Beides als Backlog-Abschnitt "Macro-Messmethodik korrigieren" oben notiert. Ausserdem geklaert: **Spooling** ist bei ICT der gerichtete Lauf zur Liquiditaet, nicht die Kompression davor — die geplante volumenbasierte Spooling-Suche entfaellt damit ersatzlos (passende Messgroesse ist der bereits erhobene Nettoweg/`dir`). Noch kein Code geaendert. |
| 2026-08-10 | **NDOG/NWOG/ORG und PDH/PDL fehlen bewusst in `macro_db.py`.** (1) `ndog_gap()`/`nwog_gap()`/`org_gap()` in `analyze_ohlc.py` filtern ueber `b.t.date() == day`, also den **Kalendertag**. Eine 1m-Session-Datei enthaelt zwei Kalendertage (18:00 Vorabend .. 17:00), dadurch wuerden die Funktionen nicht den Gap ueber die Globex-Pause messen, sondern den Sprung ueber Mitternacht — und der ist auf diesem Bestand ohnehin ein Exportartefakt (Luecke 23:59-00:08). (2) PDH/PDL und die Session-Extreme des Vortags brauchen die vorherige Tagesdatei, also Mehrdatei-Logik, die `build()` nicht hat. Level-Quelle in `macro_db` ist daher zunaechst nur `untouched_levels` (Swing-Level des laufenden Handelstags). Beides ist ein eigener Schritt. |
| 2026-08-10 | **`algo/macro_db.py` + Skill `macro-db` gebaut** (Plan: `docs/superpowers/plans/2026-08-10-macro-datenbank.md`). Eine Zeile je Macro-Fenster je Handelstag, nur vollstaendig erfasste Fenster (20/20 Kerzen + 10/10 Vorlauf). `stats` erzwingt Wilson-Intervall, Basisratenvergleich und Mindest-n=20; `plot` erzeugt drei Diagramme plus `wiki/synthesis/Macro-Datenbank (laufend).md`. In `selfcheck.py` eingehaengt. Ergebnis des ersten Laufs: siehe Wiki-Seite. |
| 2026-08-10 | **Review-Fixes `algo/macro_db.py` (Gesamt-Review Macro-Datenbank).** (1) **Kritisch:** Der Quartilsvergleich benutzte `sorted(paare)` -- bei Gleichstand im Kandidatenwert sortierte Python nach dem zweiten Tupelelement, also nach dem **Ergebnis** `dir`. `pre_streak` ist ganzzahlig (174 von 440 Zeilen haben den Wert 3), dadurch wies der Report "unterstes Quartil 20,9 % gegen oberstes 50,0 %" aus, waehrend rho=-0,004 in derselben Zeile Null sagte. Die 29 Prozentpunkte waren zu 100 % Sortierartefakt. Neue Funktion `quartile()` schneidet auf **Perzentilen des Kandidatenwerts**, gibt die echten Gruppengroessen aus und meldet, wenn eine Bindung ueber die Schnittkante laeuft -- `pre_streak` steht jetzt bei 39,1 % (n=243) gegen 41,1 % (n=197) plus Bindungswarnung. Regressionstest in `_check_stats()`. (2) Der Vergleich mass `dir >= 0,60` **ohne** die `netto_thr`-Bedingung, stellte aber die `expansion`-Basisrate 35,2 % daneben; jetzt eigene Basisrate P(dir>=0,60) = 40,0 %. (3) Kandidaten laufen jetzt gegen **drei** Zielgroessen (`dir`, `expansion`, `range`) statt nur `dir`. Dabei der **einzige belastbare Zusammenhang des ganzen Datensatzes** gefunden: `pre_range_rel` gegen `range` mit rho=**+0,258**, p=**9,5e-08** (n=417), haelt Bonferroni ueber 47 Vergleiche deutlich. Er zeigt **gegen** die Spooling-These -- nicht Ruhe, sondern Aktivitaet vor dem Fenster sagt grosse Range vorher (Volatilitaetspersistenz). Gegen `dir` unsichtbar, weil `dir` skalenfrei ist. (4) Bonferroni-Schwelle wird jetzt **angewendet** statt nur im Fusstext genannt: exakter Binomialtest je Aussage, `05:50 4,8 %` ist mit p=0,0022 gegen Schwelle 0,0011 als "haelt Bonferroni nicht" markiert. Zaehlung von 33 auf 47 Vergleiche korrigiert (Quartilsvergleiche + Level-Quoten fehlten). (5) Level-Quoten mit Basisrate: 87,5 % aller Fenster nehmen mindestens ein Level -- die Kennzahl ist fast gesaettigt, die Seitenquoten sind weitgehend Detektor-Grundrauschen (`untouched_levels`, swing=2). (6) Nullbefund steht jetzt **dauerhaft** in `wiki/synthesis/Macro-Datenbank (laufend).md` statt nur in der Konsole, plus Messergebnis-Absatz in `wiki/concepts/ICT Macros & Leading Candles.md` (Hypothese dort bewusst **nicht** geloescht, nur das Messergebnis danebengestellt). (7) Kleinkram: Diagramm 1 zeichnet Fenster unter `MIN_N` auf Hoehe 0 (23:50 mit n=1 war zuvor der hoechste Balken bei 100 %), Lookahead-Regressionstest fuer `_check_pre()` ergaenzt (einzige Messfamilie mit Blockarithmetik und bis dahin ohne), toter `NY`-Import in `backtest_macro.py`, `README.md` 72 -> 69 Bloecke, Stichproben-Vorbehalt in `SKILL.md`, p-Wert-Spalte der FVG-Tabelle im Wiki wiederhergestellt. Alle 8 Selbstchecks bestanden. Bericht: `.superpowers/sdd/2026-08-10-macro-datenbank/final-fix-report.md`. |
| 2026-08-10 | Rest der ICT-Gems-Playlist ingested (5 Videos, damit 11/11 vollstaendig). Neu fuer den Algo: (a) **Vier-FVG-pro-Stunde-These** als Backlog-Punkt — ICT behauptet, in jedem Viertelstunden-Fenster bilde sich ein FVG; Ausbleiben = High Resistance = nicht handeln. Auf `algo/backtest_macro.py --min-fvg` aufsetzbar. Vorsicht: bestehende Messung zeigt 95 % Macro-Fenster mit FVG >= 2 Punkte, die These koennte trivial wahr sein. (b) **MOC-Fensterfrage praezisiert**: eine dritte ICT-Quelle nennt wieder 15:50 (Stand 2:1 gegen 15:45) und beschreibt 15:15-15:45 als vorgelagertes Setup-Fenster — Lesart "Setup-Fenster dann Ausfuehrung", nicht als gesichert markiert. (c) **Spooling-Definition final bestaetigt**: *"all a macro is, is the beginning of a spooling event... it is not going to give you a direction"* — Macro liefert Timing, nicht Richtung. Kein Code geaendert. |
| 2026-08-10 | **`macro_db.py`: Startfehler behoben + neue Wiki-Erkenntnisse eingearbeitet.** (1) **Fehler**: Start ohne Subcommand (Run-Knopf der IDE) endete mit `argparse`-Fehler "cmd erwartet: build, stats oder plot". Jetzt laeuft ohne Argument `stats`, und `_csv_veraltet()` baut vorher neu -- wenn die CSV fehlt, aelter als die juengste Rohdatei ist **oder nicht mehr zum aktuellen Spaltensatz passt**. Letzteres ist der wichtigere Teil: kommt im Code eine Spalte dazu, lief der Report vorher stillschweigend auf dem alten Schema weiter. (2) **Exkursion ab Fenster-Open** neu erhoben (`exc_up_N`/`exc_dn_N`/`mfe_N`/`reach10_N` fuer N=20/40/60), weil ICT ausdruecklich sagt *"The move BEGINS in those 20 minutes. It's not the entirety of the move."* -- der reine Blockinhalt unterschaetzt ein Macro, das um 10:05 einen Lauf startet der bis 10:40 traegt. Richtungsagnostisch, weil ICT im selben Atemzug sagt *"It is not going to give you a direction."* **BEFUND: kein Startfenster-Effekt.** Median-MFE 48,38 / 67,50 / 82,75 Pkt, Wachstum x1,40 und x1,71 gegen die Random-Walk-Erwartung von x1,41 und x1,73 -- die Auslenkung waechst exakt wie reine Diffusion. Aus der Uhrzeit allein folgt kein Bewegungsvorteil. (3) **Nebenbefund**: ICTs Mindestziel von 10 Handles wird in **100 %** aller Fenster erreicht (n=440) -- die Schwelle selektiert nichts und taugt nur als Untergrenze fuer die Stopwahl, nicht als Filter. (4) `mfe_60` als vierte Zielgroesse aufgenommen; `pre_range_rel` haelt auch dort Bonferroni (rho +0,227, p=3,75e-06, n=405) -- der Volatilitaetspersistenz-Befund ist also nicht an `range` gebunden. (5) **Terminologie korrigiert**: die vier `pre_*`-Spalten heissen jetzt **Vorlauf-Kandidaten**, nicht Spooling-Kandidaten -- ICT meint mit Spooling den gerichteten Lauf selbst, nicht die Ruhe davor. Ein Nullbefund bei ihnen widerlegt also die alte Lesart des Begriffs, nicht Spooling. (6) MOC-Ausnahme als Vorbehalt aufgenommen: in der letzten Handelsstunde gilt das `:50-:10`-Raster laut ICT nicht (15:15-15:45 / 15:45-16:00), die Zeile `15:50` laeuft ueber den RTH-Schluss hinaus. Vergleichszahl 47 -> 54, Bonferroni-Schwelle p<0,0009. Selfcheck um `_check_excursion()` erweitert (inkl. Luecken- und Richtungsagnostik-Test). Alle 8 Selbstchecks bestanden. |
| 2026-08-11 | **Masters-Validierung in die Reports verdrahtet — Backlog 7, 8, 9 erledigt (10 bleibt offen, siehe unten).** Neues Modul `algo/confidence.py` als Bruecke zwischen dem masters.py-Werkzeugkasten und den Backtest-Reports (masters.py bleibt dadurch backtesting-Lib-unabhaengig). **(7) Bar-Renditen:** `backtest_bt.py` weist die Trades jetzt zusaetzlich auf BAR-Basis aus (`bar_metrics`/`print_bar_metrics` via `masters.bar_returns_from_trades`). Realer Lauf ueber die ersten 36 der aktuell 50 Tage (MNQ, 5m): Profit Factor **Trade 0,549 vs. Bar 0,872**, Sharpe roh Trade -0,209 vs. Bar -0,048 — Masters bestaetigt: Trade- und Bar-Kennzahl weichen systematisch ab (auf einem 8-Tage-Ausschnitt war die Trade-Zahl mit PF 1,027 sogar scheinbar profitabel, bar-basiert 0,977). **(9a) BCa-Untergrenze:** mittlere Bar-Rendite -0,0050 %, t=-0,95, 95%-BCa-Untergrenze **-0,0137 % (<= 0 -> nicht von null unterscheidbar)**; Profit-Factor-Untergrenze 0,689 (BCa auf log PF, weil das rohe PF-Ende zu schwer fuer einen Bootstrap ist). Also: die Silver-Bullet-Basisregel ohne Confluenz ist statistisch nicht von "kein Edge" zu trennen — quantifiziert, was der Backtest bisher nur qualitativ zeigte. **(9b) Drawdown:** `validate.py::monte_carlo` weist die Max-Drawdown-Zeile jetzt doppelt aus — die alte Perzentil-Zeile ist als "naiv" markiert (Masters' als "incorrect" bezeichneter Bootstrap), darunter die korrekte `double_bootstrap_drawdown()` (dd_conf 0,95/0,99) via `masters.drawdown_bound`; Selbstcheck belegt Doppel > naiv. **(8) Guard Buffer geprueft — kein Leck:** `signals.py` ist zustandslos (reine Rueckwaerts-Funktionen), Ensemble-Ziel ist die Folgetagsrichtung (Lookahead 1 -> `guard_buffer`=0), SilverBullet entscheidet nur aus `bars[t<=when]` im 1h-Fenster. `walk_forward()` trotzdem um einen `omit`-Parameter gehaertet (Default 0, byte-identisch; ein spaeter verlaengerter Zielhorizont setzt omit=H-1). Selfcheck von 8 auf **10 Checks** erweitert (`confidence`, `validate`), alle gruen. README-Abschnitte fuer `backtest_bt.py`/`validate.py` aktualisiert + neuer `confidence.py`-Abschnitt. **(10) permutation_test.py bleibt offen** — bewusst nicht mitgeliefert: es braucht je Permutation eine volle Reoptimierung, und ein Lauf dauert auf 50 Tagen ~3-5 Min, weil `SilverBulletStrategy.next()` die Historie pro Kerze als Bar-Liste neu aufbaut (O(n²)). Voraussetzung ist zuerst eine performante `next()`-Variante (inkrementeller `hist`-Aufbau, trade-population-erhaltend). Als Backlog-Punkt bei 10 oben notiert. |
| 2026-08-11 | **Forex-Erweiterung gestartet: Bulk-Download der 10 groessten Paare per Dukascopy.** Nutzerentscheidung: das Projekt wird bewusst auf Forex-Paare erweitert (bisher nur MNQ), Datenquelle bleibt `algo/fetch_dukascopy.py` (bereits vorhanden, bisher nur als 2-Tage-Test fuer EURUSD gelaufen). Nutzer wollte urspruenglich histdata.com pruefen -- verworfen zugunsten des bestehenden Dukascopy-Fetchers (Tick-Praezision, echte UTC-Sekunden bereits verifiziert, keine Fremdabhaengigkeit). Geladen: EURUSD, USDJPY, GBPUSD, USDCHF, AUDUSD, USDCAD, NZDUSD, EURJPY, EURGBP, GBPJPY als M1 von 2003-01-01 bis 2026-08-11 (max. verfuegbare Historie je Paar, fruehe Jahre liefern erwartungsgemaess leere Stunden fuer noch nicht gehandelte Paare). Nutzerentscheidung: nur M1 laden, 5m/15m/1h/4h/1d werden lokal aus M1 resampled statt zusaetzlich Tick-Daten zu laden (Groessenersparnis). Ablage `raw/marktdaten-tief/<jjjj>/<mm>/<tt.mm.jjjj>/<SYMBOL> <jjjj-mm-tt> 1m.csv` (gitignored, siehe Docstring in `fetch_dukascopy.py`). Lauf gestartet als Hintergrundprozess `algo/dukascopy_bulk.sh` (sequenziell je Paar, Log `algo/dukascopy_bulk.log`, Einzelberichte `algo/results/dukascopy_<SYM>_report.json`) -- bei ~24 Stundenabfragen/Tag ueber 23 Jahre und 10 Paaren realistisch ein mehrstuendiger bis mehrtaegiger Lauf, daher bewusst nicht synchron abgewartet. **Offen:** Resampling-Skript fuer 5m/15m/1h/4h/1d aus M1 (Reuse-Kandidat: gleiche Logik wie bestehende MNQ-Pipeline), Luecken-/Vollstaendigkeitspruefung nach Abschluss (Pflicht laut Arbeitsstandards, "Marktdaten wie Gold behandeln"), Entscheidung ob Forex ins Regelwerk (`algo/rules.py`) einfliesst oder zunaechst nur als Kontextdatenbasis dient. |
| 2026-08-11 | **Dukascopy-Bulk-Lauf abgebrochen (Nutzerwunsch), stattdessen manueller histdata.com-Import geprueft und uebernommen.** Der Hintergrundlauf aus dem vorherigen Eintrag wurde noch waehrend EURUSD/Tag 1 per `taskkill` beendet (keine unvollstaendigen Dateien entstanden, nur die 2 alten Testtage blieben). Nutzer legte stattdessen `raw/HISTDATA_COM_XLSX_EURUSD_M12000.zip` (histdata.com, Generic-XLSX-Export) manuell ab und bat um Pruefung + bedingten Import. **Pruefung:** (1) Zeitzone laut histdata.com-FAQ **feste EST (UTC-5) ohne DST** -- gegen den Wochenend-Luecken-Verlauf plausibel (Freitagsschluss durchgaengig ~16:20-16:47 unabhaengig von Sommer-/Winterzeit, passend zu einer festen statt lokal-NY-Zone). (2) Preis-Gegenprobe gegen eine unabhaengige, dokumentierte Tatsache: EUR/USD-Allzeittief **0,8225-0,8230 am 26.10.2000** -- Datei zeigt exaktes Minimum **0,8229 am 2000-10-26 03:37**, Treffer. (3) OHLC-Konsistenz (kein `low>min(o,c)`/`high<max(o,c)`), keine doppelten Zeitstempel, streng monoton -- alles sauber. **Ein echter Befund, aktiv gemeldet statt stillschweigend uebernommen:** histdata.com liefert laut eigener FAQ **Bid-Preise, nicht Mid** ("bar prices ... are based on the tick Bid price") -- das weicht von der in `fetch_dukascopy.py` bewusst gewaehlten Mid-Konvention ((bid+ask)/2, wegen IBKR-Midpoint-Bars) ab. Deshalb **nicht** stillschweigend im selben Dateiformat abgelegt, sondern eigener Dateisuffix ` 1m (bid).csv` (statt ` 1m.csv`), damit Bid- und Mid-Bestand nie versehentlich vermischt werden. **Abdeckung:** 2000-05-30 bis 2000-12-29 (182 Handelstage, 143.042 Minutenkerzen) -- Jan-Mai 2000 fehlt (vermutlich histdata.com-seitige Grenze fuer das erste EURUSD-Jahr, nicht weiter geprueft), Jahresende korrekt (29.12. war der letzte Handelstag 2000, 30./31.12. Wochenende). Neues Skript `algo/ingest_histdata_xlsx.py` (`.as_unit("s")` statt manueller Division, NY-Kalendertag-Ordnerung wie `fetch_dukascopy.py`, `--demo`-Selbstcheck). Ablage unter `raw/marktdaten-tief/2000/...` (gitignored). **Offen:** weitere histdata.com-Jahre/Paare folgen vermutlich manuell (Nutzer laedt selbst herunter); Bid-Bestand bleibt bis auf Weiteres von Backtests ausgeschlossen, bis eine bewusste Entscheidung faellt, ob/wie Bid- und Mid-Daten kombiniert werden. |
| 2026-08-11 | **`next()` von O(n²) auf O(n) umgestellt — entblockt Punkt 10.** Beide Strategien (`SilverBulletStrategy`, `EnsembleStrategy`) bauten die Historie pro Kerze komplett als `Bar`-Liste neu (`zip` ueber das ganze Fenster) — bei 11.573 5m-Kerzen quadratisch, ~270 s je 50-Tage-Lauf und der Grund, warum Permutationstests (Punkt 10) unpraktikabel waren. Neu: `backtest_bt.extend_hist(hist, data)` schreibt eine persistente `self._hist`-Liste inkrementell fort (nur die je `next()` neu sichtbare Bar anhaengen), aufgerufen VOR der Positionspruefung, damit auch Bars waehrend offener Position erfasst werden. **Ergebnis-erhaltend bewiesen, nicht nur plausibel:** (a) Vorher/Nachher-Snapshot von `SilverBulletStrategy` ueber 24 Tage — `stats._trades` (Size/EntryBar/ExitBar/Preise/PnL/ReturnPct) **Bit-fuer-Bit identisch**, 51,6 s -> 7,2 s. (b) `EnsembleStrategy` gegen einen inline rekonstruierten Alt-Neubau ueber 32 Tage — **23 Trades identisch**, 110,4 s -> 8,9 s (x12,4). (c) Voller 50-Tage-Lauf jetzt 39,9 s (vorher ~270 s); Report unveraendert: 108 Trades, WinRate 15,7 %, PF Trade 0,539 vs. Bar 0,879, BCa-Untergrenze Mittel -0,0130 % (<= 0). Dauerhafter Regressionsguard `backtest_bt.demo` (inkrementell == Neubau ueber ein wachsendes Fenster) in `selfcheck.py` eingehaengt — jetzt **11 Checks**, alle gruen. README `backtest_bt.py`/`backtest_ensemble.py` aktualisiert. |
| 2026-08-11 | **Dukascopy-Bulk-Lauf (2.) an IP-Sperre gescheitert -- Download pausiert, nicht abgeschlossen.** Nach dem Neustart aus dem vorherigen Eintrag kam trotz `--pause 1.0` weiterhin HTTP 429. `fetch_dukascopy.py` daraufhin gehaertet: `hole_stunde()` behandelt 429 jetzt mit eigenem 15s×Versuch-Backoff (statt des generischen 2^Versuch), `lade_symbol()`/CLI bekamen einen `--pause`-Parameter (Default 0.5s) fuer eine proaktive Pause zwischen JEDER Stundenabfrage, nicht nur nach Fehlern. Neustart mit `--pause 1.0` lief ~1h (186 Dateien vorher/nachher unveraendert, da Dukascopy fuer EURUSD vor Mitte 2003 ohnehin nur leere Stunden liefert -- das allein waere kein Problem gewesen). **Diagnose-Befund:** ein isolierter Einzel-Request (`versuche=1`, ganz ohne Bulk-Last) direkt nach `taskkill` des Prozesses lieferte immer noch 429 bzw. einen Verbindungs-Timeout (WinError 10060) -- das ist keine Frage der Anfragegeschwindigkeit mehr, sondern eine laengere **IP-seitige Sperre** bei Dukascopy (vermutlich Nachwirkung der drei bisherigen Laeufe/Tests heute inkl. des ersten ungedrosselten Bulk-Versuchs). Auf Nutzerwunsch ("wir sind gesperrt") **bewusst nicht weiter gegen die Sperre angetestet** (jeder Versuch koennte sie eher verlaengern als verkuerzen) -- Download bleibt bis auf Weiteres gestoppt, kein aktiver Prozess mehr. **Datenstand unveraendert seit dem letzten Log-Eintrag:** 186 CSV-Dateien (182 histdata.com-Bid-Tage aus 2000 + 2 fruehe EURUSD-Testtage 2026-08-03/04 + die kurzen Abgleichsfenster 2026-08-10/11 aus dem IBKR-Vergleich) -- **kein einziger Tag aus dem eigentlichen 2003-2026-Bulk-Ziel wurde bisher geschrieben.** **Offen:** Sperre muss serverseitig ablaufen (Zeitraum unbekannt, vermutlich Stunden); erneuter Versuch erst auf Nutzeranfrage, dann mit hoeherer `--pause` (3-6s) und ohne vorherige Test-Anfragen, die die Sperre unnoetig verlaengern koennten. |
| 2026-08-11 | **Letztes-Stunden-Macro korrekt hinterlegt: 3 statt 1 Fenster (macro_db.py).** Nutzerkorrektur: die generische Zeile `15:50` (20 Min, gebaut wie jedes andere Macro-Fenster) bildete die letzte Handelsstunde falsch ab und lief ausserdem ueber den RTH-Schluss 16:00 hinaus. Recherche im Volltext-Transkript `raw/trading-ict/2026/yt-VH7Dh1OONj4-transcript.md` ("ICT Gems - When To Anticipate Price Spooling") bestaetigt woertlich: "there's four of four macros in that last hour", belegt mit Zeiten sind aber nur zwei/drei Fenster: 15:15-15:45 (Final Hour Macro, 30 Min, unstrittig) und Market on Close -- dessen Laenge selbst zwischen Primaerquellen strittig ist (2:1 der Quellen: 15:50-16:00/10 Min; Gems-Quelle: ausdruecklich "it's not 10 minutes, it's 15 minutes, 3:45 to 4:00"). Ein vierter Teil (Algo feuert 16:01, Run bis 16:15) gilt nur zur Earnings-Saison und ist aus OHLC allein nicht erkennbar. Statt den Streit einseitig aufzuloesen: neue `LAST_HOUR_WINDOWS`-Konstante ersetzt die Zeile 15:50 durch drei eigene Fenster (15:15/30min, 15:45/15min, 15:50/10min) mit je eigener Laenge; `is_complete()`/`build()` lesen die Fensterlaenge jetzt aus `end-start` statt fest `WINDOW_MIN=20` anzunehmen. `N_WINDOWS` 23->25, CSV neu gebaut (483 statt 461 Zeilen ueber dieselben 23 Handelstage), `--selfcheck` gruen (Assertions auf variable Fensterlaenge umgestellt). Wiki (`Market on Close (MOC) Macro Model.md`) und `.claude/skills/macro-db/SKILL.md` entsprechend ergaenzt. |
| 2026-08-11 | **Bug in `fvgs()` gefixt: VII wurde bei der FVG-Grenzziehung komplett ignoriert.** Nutzerkorrektur mit konkretem Zahlenbeispiel (Close1 29.826,00 -> Open2 29.826,50 -> Close2 29.830,00 -> Open3 29.830,75 => BISI-Zone 29.826,00-29.830,75). `tools/analyze_ohlc.py::fvgs()` zog die Grenze bisher ausnahmslos aus den Wicks von Candle1/Candle3 (`a.h`/`c.l` bzw. `c.h`/`a.l`) -- `viis()` existierte komplett unabhaengig daneben, ohne dass `fvgs()` sie je konsultierte. **Fix:** an jeder Seite wird jetzt geprueft, ob zur mittleren Kerze eine VII vorliegt (`m.o` vs. `a.c` bzw. `c.o` vs. `m.c`, dieselbe Bedingung wie in `viis()`); liegt eine vor, wird der aeussere VII-Rand (Close Candle1 / Open Candle3) statt des Wicks als Grenze genutzt -- ohne VII bleibt der alte Wick-Fallback. Neuer Regressionstest `tools/test_fvg_vii.py` (BISI mit VII, SIBI mit VII, kein VII) faengt genau den vorher stillschweigend falschen Fall (Wick jenseits des VII-Rands) ab; vor dem Fix schlug er fehl (`lo=29827.0` statt `29826.0`). **Verifiziert:** `algo/selfcheck.py` weiterhin 11/11 gruen; alle 7 `fvgs()`-Konsumenten (`backtest_ohlc.py`, `backtest_fvg_specialness.py`, `backtest_midnight_range_judas.py`, `backtest_macro.py`, `macro_db.py`, `rules.py`, `live_status.py`) einzeln durchlaufen lassen -- keiner reimplementiert die Grenzlogik separat, alle nutzen die zentrale Funktion, kein struktureller Bruch, keine Fehler. **Auswirkung:** jede bisherige FVG-Groesse/C.E./Fill-Auswertung mit beteiligter VII war zu klein (Grenze zu eng am Wick statt am VII-Rand); da alle Konsumenten `fvgs()` importieren statt selbst zu rechnen, wirkt der Fix automatisch ueberall, absolute Kennzahlenverschiebung nicht separat quantifiziert (kein Alt/Neu-Vergleichslauf angefordert). |
| 2026-08-11 | **These (Jannes, aus dem NY-SB-Tapereading 11.08.): "gab es gute Bewegung aber konnte erst ab 16 Uhr [= 10:00 NY] aktiv was machen"** — also die Vermutung, dass ein starker RTH-Open-Drive (09:30–09:50) die Silver-Bullet-Stunde (10:00–11:00) leerlaufen laesst. Neues Skript `algo/backtest_open_drive_vs_sb.py` (reused: `tools.analyze_ohlc.load/at`, `backtest_macro.session_day_from_path`, `backtest_common.write_result`; eigener Selbstcheck mit Kunst-Tag Drive-vs-Chop und Split-Grenzen). Getestet werden zwei getrennte Fragen: wird die SB-Stunde nach starkem Open **kleiner** (range) und wird sie **richtungsloser** (dir = \|netto\|/range). Split: oberes Drittel der Open-Range gegen den Rest, Mann-Whitney-U. **Ergebnis: kein Signal, These nicht gestuetzt.** n=21 Tage (2026-07-08 bis 2026-08-06), Schwelle 254 Pkt. SB-Range nach starkem Open Median **223** vs. **240,6** im Rest (p=**0,332**); SB-Direktionalitaet **0,46** vs. **0,42** (p=**0,689**). Beide Richtungen null; der numerische Trend bei der Direktionalitaet geht sogar *gegen* die These (nach starkem Open eher gerichteter, nicht choppiger). **Belastbarkeit gering:** 21 Tage sind zu wenig fuer eine Aussage in beide Richtungen — die These ist damit nicht widerlegt, sondern unentschieden, und gehoert bei wachsendem 1m-Bestand erneut gefahren. Zeitfenster gegen die Rohdaten verifiziert (Bars tragen UTC-04:00, 60 Bars je SB-Stunde, Fragmenttag 07.08. mit 26 Bars korrekt ausgeschlossen). **Datenlage-Warnung:** MNQ-1m endet am **2026-08-07**; fuer 10.08. und 11.08. liegen in `raw/marktdaten/` nur Forex-Dateien, kein MNQ/NQ — genau die beiden Tage, die Jannes gerade dokumentiert hat, fehlen im Backtest-Bestand. |
| 2026-08-11 | **Nachtrag zur Open-Drive-These + Datenluecke geschlossen.** Auf Jannes' Anweisung ("hole dir immer die aktuellen Daten, wenn ich mit diesem Skill arbeite, und das automatisch") ist das Nachladen der Marktdaten jetzt **Schritt 0** im `trading-journal`-Skill (`~/.claude/skills/trading-journal/SKILL.md`) — vor dem Lesen der Screenshots, ohne Rueckfrage, mit `ls`-Gegenprobe statt Exit-Code-Vertrauen. `python algo/fetch_yfinance.py 2026-08-06 2026-08-11` hat 11 Dateien geschrieben, MNQ-1m reicht jetzt bis **2026-08-10** (08.08. ist ein Samstag, korrekt leer; 11.08. fehlt weiter, weil `end` exklusiv ist und die Session noch lief — laut `--help` beabsichtigt, kein Bug). **Datenqualitaet verifiziert:** 1369 Bars, 2026-08-09 18:10 bis 2026-08-10 16:59, Zeitstempel tragen UTC-04:00; unabhaengige Gegenprobe ueber das 1m-Vakuum vom 10.08. — liegt exakt bei 12:31→12:32 NY (30,75 Pkt Close→Open) und bestaetigt damit die Zeitachse gegen eine Beobachtung, die nicht aus diesen Daten stammt. **`backtest_open_drive_vs_sb.py` mit n=22 erneut gelaufen: weiterhin kein Signal**, Schwelle 227,75 Pkt, SB-Range 223,6 vs. 240,6 (p=**0,453**), SB-Direktionalitaet 0,47 vs. 0,41 (p=**0,973**). Der eine zusaetzliche Tag verschiebt die Schwelle und schwaecht beide p-Werte — genau das Verhalten, das bei n≈20 zu erwarten ist und das die Zahlen als noch nicht belastbar ausweist. |
| 2026-08-11 | **Korrektur der Tagesbewertung 11.08. — Bias war richtig, ich hatte ihn falsch bewertet.** Jannes' Klarstellung: sagt er „Bias", meint er **Daily oder Weekly**, nicht Session; ein Session-Bias kommt selten vor und wird dann genannt. Ich hatte den `tape-reading`-Bias aus einer Beobachtung *innerhalb* der Session abgeleitet und implizit gegen den Verlauf der SB-Stunde bewertet — falscher Massstab. **Gegen die Rohdaten geprueft** (`MNQ 2026-08-11 1d.csv`, nach Sessionende nachgeladen): O 29 764,25 · H 29 887,00 · L 29 573,00 · C 29 612,00 = **−152,25 Pkt, baerish**. `bias_korrekt: ja` in beiden Eintraegen und in der CSV nachgetragen. **Zweite Korrektur, wichtiger:** ich hatte berichtet, die SB-Stunde sei „gegen ihn" gelaufen und er habe die Richtung verpasst. Falsch. Der Move kam im **Macro 10:50-11:10**: Hoch der zweiten Tageshaelfte 10:52 bei 29 805,00, sein Ausstieg 10:56 bei 29 796, dann 11:05-11:09 Abverkauf 29 777 → 29 712 (**65 Pkt in vier Minuten**), Stunde 11:00-12:00 netto **−114,25** Pkt bei dir 0,85, Tagestief 29 573 = **223 Pkt unter seinem Ausstiegskurs**. Er hatte um 10:45 selbst „5min bis zum naechstem Macro" notiert. Richtige Bewertung des Tages: Bias richtig, Setup-Disziplin richtig, **Ausdauer neun Minuten zu kurz**. Neuer Fehlerkatalog-Eintrag **P12 „Fenster vor Ablauf verlassen"** mit diesem Tag als Erstbeleg; Vokabelregel in `references/fields.md` ergaenzt (Bias = Daily/Weekly, `bias_korrekt` gegen den Daily-Candle-Close werten, nicht gegen das Handelsfenster). |
| 2026-08-11 | **Bug in `rules.py::plan_trade` gefixt: das "erste FVG im Fenster" war in 39 % der Faelle randueberlappend.** Nutzerkorrektur (Jannes): *"Das 1.p FVG einer jeder Session muss innerhalb der Session liegen, also ist das fruehstmoegliche FVG in NY AM 9.30-9.31-9.32 3 candle stick formation."* Der Code filterte bisher `[g for g in fvgs(hist) if win_start <= g["t"] <= when]` -- `g["t"]` ist aber die **mittlere** Kerze der Formation. Ein FVG mit Mitte exakt auf `win_start` (10:00) beginnt real um 09:59, also **vor** dem Fenster, und wurde trotzdem als erstes FVG der Session genommen. **Fix:** Fenster vor der Detektion schneiden (`fvgs([b for b in hist if b.t >= win_start])`) statt danach auf die mittlere Kerze zu filtern -- damit liegt die ganze 3-Kerzen-Formation garantiert innen. Kein Lookahead, `win_bars` bleibt Teilmenge von `bars[t<=when]`. **Auswirkung auf echten Daten (25 MNQ-1m-Tage, 71 Silver-Bullet-Fenster): 28 Fenster (39,4 %) hatten ein randueberlappendes erstes FVG**, in **11 davon kippt die Handelsrichtung** (long<->short, z.B. 2026-07-29 NY PM bullish C.E 27 784,50 -> bearish C.E 27 835,75); Entry-C.E.-Differenzen bis **67,75 Punkte** (2026-07-17 NY AM). Die alten Treffer lagen fast ausnahmslos exakt auf 03:00/10:00/14:00 -- plausibel, weil das 09:50-10:10-Macro sein Displacement genau ueber die Fenstergrenze legt. Neuer Regressionsfall in `rules.py::demo()` (randueberlappendes FVG bei 09:55/10:00/10:05 gegen das innenliegende 10:10/10:15/10:20); ohne den Fix schlaegt er fehl (C.E 98,5 statt 103,5). `algo/selfcheck.py` 11/11 gruen. **Nicht betroffen:** `macro_db.py` (FVGs als Vorgeschichte vor dem Fenster, kein "erstes der Session"), `live_status.py` und `analyze_ohlc.py::day_report` (kein Session-Erstes). **Offen:** alle bisherigen Silver-Bullet-Backtestzahlen stammen aus der fehlerhaften Auswahl und sind neu zu fahren. |
| 2026-08-11 | **Neu: `algo/mor_levels.py` — Einzeltag-Readout der Midnight Opening Range** (0:00-0:30 NY): Midnight Open, Range High/Low, Quarters, STD-Projektionen ±0,5 bis ±5 und die „first presentation" (erstes FVG *innerhalb* der Range, Definition aus `wiki/concepts/Midnight Opening Range.md`). Reuse: `session_range()` aus `backtest_midnight_range_std.py`, `fvgs`/`at`/`load` aus `tools/analyze_ohlc.py`; eigener `--selfcheck`. **Designentscheidung:** statt einer festen Punkt-Toleranz („Extrem lag auf dem Level") wird das **naechste** Level je Extrem ausgewiesen, mit Abstand in Punkten **und** in Range-Anteilen. Grund: die erste Fassung nutzte 15 Pkt absolut und meldete bei der 18,75-Pkt-Range vom 11.08. vier Level gleichzeitig als Treffer — das taeuscht Praezision vor, die es nicht gibt. **Auswertung 11.08.:** MOR 29 836,00-29 854,75, Range nur **18,75 Pkt**, Midnight Open 29 839,25; first presentation ein bullishes 2,5-Pkt-FVG (29 841,00-29 843,50), entstanden 00:10, bereits 00:23 gefuellt — als Tagesreferenz wertlos. **Antwort auf Jannes' Frage (wurde High oder Low per STD festgelegt): nein, keins von beiden.** Tages-High 29 887,00 (07:04) liegt bei k=1,72, naechstes Level +1,5 STD mit 4,12 Pkt Abstand — sieht nach Treffer aus, ist aber **schlechter als Zufall**: bei 18,75 Pkt Range stehen die STD-Level 9,38 Pkt auseinander, die Zufallserwartung zum naechsten Level betraegt 2,34 Pkt. Tages-Low 29 573,00 (13:15) liegt bei **k=14,03**, also 169 Pkt unter −5 STD. **Nebenbefund gegen die Wiki-These:** die „maximale Manipulation bis −1 STD waehrend London" hielt heute nicht (London-Tief 29 717,50 = **−6,32 STD**) — und laut `backtest_midnight_range_std.py` ist das der Normalfall, nicht die Ausnahme: **52,3 % aller Tage (n=44) reissen −1 STD in London**. Ein −1-STD-Bruch taugt damit nicht als Bias-Signal; ich hatte ihn kurz dafuer gehalten und vor dem Berichten gegengeprueft. **Datenwarnung:** `MNQ 2026-08-11 1m.csv` wurde 13:40 NY bei laufender Session geschrieben und heilt nicht von selbst (`write_day()` ueberschreibt nie) — nach 17:00 NY loeschen und neu ziehen, sonst bleibt der Tag dauerhaft abgeschnitten. |
| 2026-08-11 | **These (Jannes/ICT): "1.p FVGs sollen besonders sein, gerade das der Montags-NY-AM-Session soll die gesamte Woche relevant sein" -- geprueft, Ergebnis unentschieden.** Vorab Definitionspraezisierung durch Jannes: ein vor Sessionbeginn gebildetes FVG **zaehlt weiter als normales FVG**, es ist nur kein 1.p FVG (Kommentar in `rules.py` entsprechend ergaenzt, damit der Fix von heute nicht als "FVG ungueltig" missverstanden wird). Neues Skript `algo/backtest_1p_fvg_woche.py` (reuse: `analyze_ohlc.load/at/fvgs`, `backtest_common.write_result`; eigener `--selfcheck` mit Kunstdaten, der genau die Randformation 9:29/9:30/9:31 gegen 9:30/9:31/9:32 prueft). Drei Tests: **(A)** Touch am unmittelbaren Folgetag, Mo gegen Di/Mi/Do (auf einen Tag normiert, sonst waere der Montagsvorsprung reine Exposure) -- 5m **56 % (5/9) vs. 50 % (11/22), p=1,00**; 1m **75 % (3/4) vs. 43 % (6/14), p=0,58**. **(B)** Montags-FVG irgendwann Di-Fr beruehrt -- 5m **78 % (7/9)**, 1m **100 % (4/4)**, erster Touch ueberwiegend **Dienstag** (5/7 bzw. 3/4), aber ohne Kontrollgruppe. **(C)** 1.p FVG gegen die uebrigen FVGs derselben Session -- 5m **52 % vs. 62 % (p=0,40)**, 1m **50 % vs. 63 % (p=0,32)**. **Bewertung: weder bestaetigt noch widerlegt**, kein Test signifikant, 9 bzw. 4 Montage sind zu wenig. Zwei Details fuer die Weiterverfolgung: Montags-FVGs liegen im Median **weiter** vom Tagesschluss weg (214 vs. 176 Pkt bzw. 284 vs. 183) und werden trotzdem mindestens gleich oft angelaufen -- das ist der einzige Datenpunkt **fuer** die These; und der (C)-Rueckstand des 1.p FVG ist durch dessen groesseren Abstand (197 vs. 135 Pkt in 1m) plausibel erklaert, also **kein** Gegenbeleg. Nebenbefund: Donnerstags-1.p-FVG wird freitags auffallend selten angelaufen (5m 1/7). Als offene Hypothese in `wiki/synthesis/Muster-Validierung (laufend).md` hinterlegt, Wiederholung ab ~20 Montagen in 1m. |
| 2026-08-11 | **DATENFEHLER GEFUNDEN — yfinance liefert 00:00-00:08 NY nicht, jede Midnight-Range-Zahl war falsch.** Jannes meldete, der Midnight Open sei 29 832,25, nicht die von mir gemeldeten 29 839,25. Ursachensuche: in `MNQ 2026-08-11 1m.csv` fehlen die Kerzen **23:59 bis 00:08** — mein Code nahm „erste Kerze ab 0:00" und las den Open der **00:09**-Kerze als Midnight Open. Sein Wert passt exakt an den 23:58-Close (29 834,00) an. **Systematisch, nicht Einzelfall:** an **19 von 24** MNQ-1m-Tagen fehlen exakt dieselben neun Minuten inkl. der 0:00-Kerze. **Gegen den rohen `yf.download()`-Abruf verifiziert: die Luecke steckt in der Quelle, nicht in `fetch_yfinance.py`** — unser Downloader gibt sie treu weiter. Auch die 5m-Datei ist betroffen (00:00-00:05-Kerze fehlt), also kein Ausweg ueber eine groebere Aufloesung. **Auswirkung:** eine aus 21 von 30 Minuten gerechnete Opening Range faellt zu klein aus; da alle STD-Level Vielfache der Range sind, sind saemtliche k-Kennzahlen aufgeblaeht. **Meine Zahl von vorhin ist damit hinfaellig:** „London-Low reisst an 52,3 % der Tage −1 STD" (n=44) war ein Artefakt; auf sauberen Daten bleiben **n=10** und die Richtung dreht sich (London-Low Median 0,00 STD bei n=5 im 1m-Bestand). Die Basisrate ist derzeit schlicht **unbekannt**. **Fix (Korrektheit vor Features, direkt umgesetzt):** neue `window_gaps()` in `backtest_midnight_range_std.py` als geteilter Riegel; `session_range()` bekommt `expect_complete=True` und verwirft loechrige Fenster; `midnight_range()` nutzt das; `mor_levels.py` bricht mit erklaerender Meldung ab statt zu rechnen (**bewusst kein `--force`** — falsche Zahlen sind hier schaedlicher als gar keine); `backtest_midnight_range_std.py` und `backtest_midnight_range_judas.py` weisen die Zahl verworfener Tage aus. **Zwei Folgefehler im Riegel selbst gefunden und behoben, bevor sie stehen blieben:** (1) ein komplett leeres Fenster wurde als „alle Minuten fehlen" gezaehlt, wodurch jeder 1d-only-Tag als Datenluecke galt — `window_gaps()` gibt fuer leere Fenster jetzt `[]` zurueck; (2) `find_days()` faellt auf **5m** zurueck, wenn keine 1m-Datei existiert, und die in Minuten gerechnete Pruefung hielt die 6 Kerzen eines 5m-Fensters fuer 24 fehlende Minuten — neue `bar_minutes()` leitet den Kerzenabstand als Modalwert aus den Daten ab. Danach stimmen die Verwurfszahlen mit der unabhaengigen Handmessung ueberein (NY Pre 7:00-7:30: 0 verworfen, NY PM 13:30-14:00: 1, Midnight: 34 von 44). `algo/selfcheck.py` nach jedem Schritt gruen (11/11). **Offen:** MOR nur ueber TradingView-Export oder spaeter IBKR korrekt zu bekommen; die konkrete Range vom 11.08. bleibt bis dahin unbekannt (bekannt ist nur Open 29 832,25 von Jannes und High 29 854,75 aus dem intakten Teil). |
| 2026-08-11 | **These (Jannes): "wird mit STD ein High/Low fix gesetzt -- entweder Session oder Daily High/Low?" -- erster Backtest, aktuell NICHT gestuetzt.** Neues Skript `algo/backtest_org_std_extrema.py` (reuse: `analyze_ohlc.load/at`, `backtest_common.write_result`; eigener `--selfcheck`). Geprueft werden **beide** STD-Basen, die das Wiki kennt: **org** = Gap Vortagsschluss 16:14 -> Open 9:30, und **or_** = Opening RANGE 9:30-10:00 High/Low (*"liefert die Ziele des Tages"*). Bei der or_-Basis zaehlen nur Extrema **ab 10:00**, sonst waere der Treffer zirkular (die Range definiert ihr eigenes High/Low). Projektion in 0,5er-Schritten bis 3,0 STD beidseitig. **Kernpunkt des Aufbaus ist die Nullerwartung**: die Level stehen im Abstand 0,5*Gap, ein Trefferfenster von +-5 % deckt damit schon **20 %** der Preisachse ab -- ohne diese Baseline wuerde jede Trefferquote hoch aussehen. **Ergebnis: keine einzige der 8 Kennzahlen liegt signifikant ueber dem Zufall.** 5m (42 Tage): org 5-17 %, or_ 14-21 %, alle p>=0,47. 1m (23 Tage): org 9-17 %, or_ 13-26 %, bestes p=0,305 (`or_:post_daily_low` 26 %). Der **Median-Abstand** zum naechsten STD-Level liegt mit 0,10-0,20 STD ueberwiegend **ueber** dem Zufallserwartungswert 0,125 -- die Extrema liegen also eher weiter weg von STD-Leveln, nicht naeher dran. Sensitivitaet geprueft (tol=0,03/0,05/0,10 gegen Erwartung 12/20/40 %): Bild unveraendert, das Ergebnis haengt nicht an der Toleranzwahl. **Bewertung:** mit 42 bzw. 23 Tagen zu wenig fuer ein endgueltiges Urteil, aber es gibt aktuell **keinen** Hinweis, dass ORG- oder Opening-Range-STDs Session- oder Daily-Extrema fixieren. Als offene Hypothese hinterlegt, Wiederholung bei groesserem 1m-Bestand. Datenpunkt vom Tag selbst (11.08.): Tagestief 29 533,50 liegt zwischen -2,0 STD (29 554,75, erreicht) und -2,5 STD (29 507,25, offen), also **nicht** auf einem Level. |
| 2026-08-11 | **Richtigstellung meiner Fehlinterpretation von "ORG NY AM".** Jannes' Nachfrage "ich meine die ORG NY AM" habe ich als *Opening Range 9:30-10:00* gelesen und daraufhin faelschlich notiert, die "richtige STD-Basis" sei die Range statt des Gaps. Jannes hat das klargestellt: **"Das Opening Range Gap heisst der Closing Price vom gestrigen Tag um 16:14 und der Opening Price um 9:30"** -- "NY AM" praezisierte also nur, **welches** ORG gemeint ist (NY AM gegen PM 13:30-14:00, London, Midnight), nicht eine andere Range. **Gueltig bleibt damit die urspruengliche ORG-Rechnung fuer den 11.08.:** Anker 16:14-Close **29 744,75** -> 9:30-Open **29 839,75**, Gap **95,00 Pkt**, C.E. **29 792,25**, 1/16 = 5,9375 Pkt; Gap-Fill bereits 09:31, ab 11:07 endgueltig nach unten verlassen. Der separat gemessene Befund zur Opening Range (-0,5 STD 29 532,62 vs. Tagestief 29 533,50, 0,88 Pkt) bleibt als **eigenstaendiger** Datenpunkt bestehen -- er ist nicht falsch, war aber der falschen Frage zugeordnet. Fuer die STD-These heisst das: **beide** Basen bleiben offen und werden weiter parallel gefahren (`backtest_org_std_extrema.py` testet ohnehin beide), keine ist durch Jannes' Aussage ausgeschlossen. |
| 2026-08-11 | **Tick-Raster projektweit hinterlegt und erzwungen -- 34 % der Entries und 88 % der Stops waren nicht platzierbare Preise.** Nutzerkorrektur (zweimal noetig): *"der nq bewegt sich immer in 0,25 punkte schritten, daher koennen keine preise wie 0,28 oder 0,44 existieren"*. **Ursache:** es gab im Projekt keine Tick-Groesse. Zwei Quellen erzeugten Phantasiepreise -- (1) der C.E. ist ein Mittelwert `(lo+hi)/2` und landet zur Haelfte zwischen zwei Ticks (**50 % aller FVG-C.E.**, 653 von 1310 in der Stichprobe), (2) der Stop-Puffer ist ein Prozentwert und erzeugt beliebige Nachkommastellen. **Messung vor dem Fix ueber 624 Setups aus allen 25 1m-Tagen: Entry 214 ungueltig (34 %), Stop 546 (88 %)**, Ziele sauber (kommen aus echten Swing-Preisen). Ein Backtest simulierte damit Fills an Preisen, die nie gehandelt wurden; IBKR haette die Orders abgewiesen oder still gerundet. **Fix:** `TICK_SIZE` + `to_tick()`/`tick_of()` in `tools/analyze_ohlc.py` als **einzige Quelle der Wahrheit** (stdlib-only, unterste Schicht; MNQ/NQ/ES/MES 0,25, Forex 0,00001, JPY-Paare 0,001); `algo/pnl.py` importiert von dort statt eine zweite Tabelle zu fuehren und reicht sie als `round_to_tick()` weiter. `fvgs()` und `org_gap()` bekamen einen `tick`-Parameter und runden den C.E.; alle Konsumenten (`rules.py`, `macro_db.py`, `mor_levels.py`, `live_status.py`, `analyze_ohlc.day_report`) uebergeben ihn. In `rules.py::plan_trade` wird **gerichtet** gerundet, damit die Rundung nie zugunsten des Backtests ausfaellt: Entry schwerer zu fuellen, Stop weiter weg. **Verifikation nach dem Fix: 6338 FVG-C.E. + 624 Setups (Entry/Stop/Ziel) = 0 ungueltige Preise.** Regressionstests in `pnl.demo()` (Rundungsrichtung, .5-Fall, rasterkonforme Preise duerfen sich nicht verschieben, unbekanntes Symbol fliegt auf) und `rules.demo()`; `selfcheck.py` 11/11 gruen. Dauerhaft hinterlegt in `wiki/concepts/Kontraktspezifikation MNQ (Tick, Punktwert).md` und im Memory. **Folgefund, bewusst NICHT selbst entschieden:** nach der Rundung sind **23 % der Setups mit <= 2 Punkten Risiko** unterwegs, 3 % mit genau 1 Tick ($0,50/Kontrakt) -- Median-RR 7,9 bei einem Maximum von **522**, 32 Setups ueber RR 100. Das sind Divisionsartefakte durch einen Nenner nahe null und blaehen jeden Erwartungswert auf. Ein Mindest-Stop-Abstand ist eine Handelsentscheidung, keine Bugkorrektur -- wartet auf Jannes' Zahl. |
| 2026-08-12 | **Marktdaten-Bestandsaufnahme + drei Quellenbefunde** (Anlass: Jannes' Wunsch nach vollstaendiger 1m-Historie, einheitlichem Loader und einem manuell ausloesbaren Nachlad-Skill). **(1) Bestand gemessen:** 1m gibt es nur 25 Tage (MNQ), 35 (ES), 33 je Forex-Paar -- exakt das rollierende 30-Tage-Fenster von yfinance. Tief ist ausschliesslich 1d (Forex ab 2000, 5.264-6.928 Tage je Paar). Eine 1m-Historie ueber Jahrzehnte existiert im Vault **nicht**; DXY existiert gar nicht (0 Dateien). **(2) MNQ-Mitternachtsluecken bestaetigt:** 06.08. 23:58->00:09 (11 Kerzen), 10.08. 23:58->00:00, 11.08. 23:58->00:09 -- betrifft den Midnight Opening Price 00:00 NY. Gegenprobe EURUSD/GBPUSD/USDJPY: an denselben Tagen **keine** Luecke (1.380/1.408 Kerzen). Also MNQ-spezifisch, vermutlich echte Marktstille im Micro-Kontrakt (yfinance schreibt fuer Minuten ohne Trade keine Kerze) -- deckt sich mit Jannes' Notiz vom 10.08. Separat davon ein **echter Abbruch**: MNQ 2026-08-07 1m hat 617 statt ~1.380 Kerzen und beginnt erst 00:09. Beide Faelle sehen in der Datei identisch aus -- keine Datei traegt Herkunft oder Vollstaendigkeitsstand. **(3) Dukascopy antwortet HTTP 429** (geprueft ~28 h nach dem abgebrochenen Bulk-Lauf vom 11.08.); sequenziell braeuchte der Downloader ~40 h je Paar, ~400 h fuer zehn -- erreicht sein Ziel nie. **(4) TradingView-MCP-Server fuer CME-Index-Futures unbrauchbar:** futures_category_snapshot(equity_index) lieferte 1 von 6 Kontrakten (nur Nikkei), ES/NQ/RTY/YM/EMD fehlten, >120 s Laufzeit, Fehlen nur an "returned": 1 erkennbar. Nicht in die Pipeline genommen. Konsequenz: Design-Spec docs/superpowers/specs/2026-08-12-marktdaten-schicht-design.md (Torwaechter data_gate.py, Herkunfts-Manifest, 1m-Vorrang nach Zielbild-Spec 4.3, Skill /marktdaten). Tiefhistorie bewusst separat. |
| 2026-08-12 | **TradingView-Exportweg gemessen und automatisiert** (Browsersteuerung ueber Chrome, erstmals genutzt). Fuenf Befunde: (1) Der Export liefert alle *geladenen* Balken, nicht den sichtbaren Bereich — ein naiver Export ergab 303 statt 1380 Kerzen. Ladeschritt ist das Zeitbereich-Werkzeug (Go to > Custom range), **nicht** die Presets 1D/5D: die stellen die Zeiteinheit um. (2) Die MNQ-**Mitternachtsluecken sind ein Exportartefakt, kein Marktphaenomen** — ein neuer Export desselben Zeitraums liefert die zehn fehlenden Kerzen (10.08. 23:59–11.08. 00:08); die Spec-Annahme 'echte Marktstille' (2026-08-12-marktdaten-schicht-design.md §6.4) ist damit widerlegt. Der frueher als 'abgebrochener Download' eingestufte 07.08. hat dieselbe Ursache. (3) **TradingView revidiert Kurse nach**: 74 von 1171 Kerzen (6,3 %) weichen zwischen zwei Exporten desselben Tages ab, fast nur open/close, Median 1 Tick, max 8 Punkte — Merge braucht darum die Regel 'spaeterer Export gewinnt'. (4) **Teilexporte ergaenzen sich zu vollstaendigen Tagen**: MNQ 11.08. aus vier Teilexporten = exakt 1380 lueckenlose Kerzen; damit ist der Sollwert 1380 fuer Futures gemessen statt geschaetzt. (5) Im Layout lauern zwei stille Fallen: **RTH** (nur 09:30–16:00) und **B-ADJ** (rueckangepasste Continuous-Serie bei `MNQ1!` statt echter Kontraktpreise) — beide muessen auf ETH bzw. den echten Kontrakt (`MNQU2026`) stehen. Neu: `algo/ingest_tvexport.py` (Split nach Handelstagen, Merge, Lueckenbericht, Selbstcheck). |
| 2026-08-12 | **Forex-1m-Bestand ist zu grossen Teilen unbrauchbar** (ungefragt gemeldet, Nulltoleranz-Regel). Anteil Kerzen mit `open=high=low=close`: AUDUSD 100 %, GBPUSD 100 %, NZDUSD 100 %, EURUSD 93,9 % — zusammen ~90.000 Attrappen-Kerzen ohne Range, auf denen weder FVG noch Displacement noch Sweep messbar ist. Die uebrigen Paare liegen bei 0,0–0,3 % (normale Flat-Minuten). Ursache ist der yfinance-Nachlad; Werte wie `1.1547343730926514` sind Float-Rauschen, kein Kursformat. Gegen einen OANDA-Export weichen 417 von 763 gemeinsamen EURUSD-Kerzen um 2,5–5,3 Pips ab. **Folge fuer die Spec:** `data_gate.py` braucht eine siebte Pruefung 'Anteil Nullrange-Kerzen' — die sechs geplanten haetten das durchgelassen, weil `h >= max(o,c)` bei `O=H=L=C` formal erfuellt ist. Auf Wunsch des Nutzers wird Forex vorerst nicht weiterverfolgt (Fokus: MNQ, ES, YM, DXY); der Bestand bleibt unveraendert liegen. |
| 2026-08-12 | Datenstand nach dem ersten automatisierten Exportlauf: **MNQ 11.08. = 1380 (vollstaendig)**, **ES 10.08. und 11.08. = je 1380 (vollstaendig)**, MNQ 10.08. = 1369, DXY 10.–12.08. eingelesen. **DXY hat systematische Luecken** (9 Stueck, darunter zweimal ~87 min um 18:04 NY) und wird nie 1440 Kerzen erreichen — das Sollwert-Profil muss pro Symbol gelten, ein globaler Vollstaendigkeitsbegriff waere fuer DXY unerfuellbar. YM (`YMU2026`) steht aus: Chrome blockiert nach mehreren Exporten weitere Downloads von tradingview.com. Punktwert YM = $5 in `pnl.py`, Tick = 1 Indexpunkt in `analyze_ohlc.py` eingetragen (MYM gleich mit), `selfcheck.py` weiter 11/11. |
| 2026-08-12 | **YM (`YMU2026`) nachgezogen**, damit sind MNQ, ES, YM und DXY eingelesen. YM 11.08. = 1326 von 1380 Kerzen bei 48 Luecken — davon **43 exakt eine Minute lang und alle zwischen 18:00 und 05:00 NY, keine einzige in der RTH-Session**. Das ist echte Marktstille im duenn gehandelten Dow-Future, nicht das Exportartefakt, das bei MNQ vorlag (dort: Luecke verschwand im zweiten Export, hier: Verteilung folgt dem Volumenprofil). **Folge:** ES und MNQ erreichen 1380 lueckenlos, YM und DXY koennen das strukturell nicht. Die Nutzer-Regel "nur vollstaendige Tage backtesten" braucht darum einen symbolspezifischen Vollstaendigkeitsbegriff (Sollwert je Symbol statt global 1380/1440), sonst fallen YM und DXY dauerhaft aus jedem Backtest heraus. Noch offen, gehoert in `data_gate.py`. |
| 2026-08-12 | **Neue Daily-Bias-Notiz `raw/daily bias 12.08.md` verarbeitet** (CPI 8:30 NY als Volatilitaetstreiber, Buyside-Weekly-DOL 30 094,00, ORG-C.E.-70%-These live vor RTH-Open angewendet). `algo/live_status.py` frisch gezogen zur Verifikation: 29 800,00 um 05:00 NY, deckt sich mit Jannes' 29 804 (11:00 CEST) — `org_ce` liefert vor 9:30 NY erwartungsgemaess `null`, die eigentliche ORG steht erst mit RTH-Open fest. Jannes' These (grosser Gap → C.E. fuellt seltener innerhalb 30 Min) als Backlog-Idee (Gap-Segmentierung in `backtest_org_ce.py`) und als Nachtrag in `wiki/synthesis/Muster-Validierung (laufend).md` hinterlegt; nach 9:30 NY mit echten Daten nachpruefen. |
| 2026-08-12 | **Ingest 13 MIT-15.S08-Quant-Finance-Transkripte** (`raw/quant-finance/2026/`) — Formeln aus Linearer Algebra, Wahrscheinlichkeitstheorie/Stochastik, Regression, PCA, Bond-Mathematik, Portfolio-Management, Counterparty-Risk ins Wiki uebernommen (7 Konzeptseiten + 13 Sourceseiten + 1 Synthese-Seite, siehe `wiki/synthesis/Quant-Finance-Formeln für den MNQ-Algo (laufend).md`). **Neue, noch nicht getestete These aus Lecture 6** (automatisch geloggt gemaess Arbeitsstandard "jede neue These wird automatisch geloggt und gebacktestet"): Markov-Ketten-Uebergangswahrscheinlichkeiten fuer MNQ-Bar-Zustandsfolgen (Up/Down-Sequenzen, analog zum Apple-Beispiel der Vorlesung: aufeinanderfolgende Aufwaertstage koennen die Wahrscheinlichkeit eines weiteren Aufwaertstages erhoehen) — noch kein Backtest-Skript gebaut (`algo/backtest_markov_bars.py`, geplanter Dateiname), folgt in einer kommenden Session. Weitere konkrete Einsatzideen (PCA-Multi-Timeframe-Feature fuer `algo/signals.py`, Gain-Loss-Ratio-Kennzahl fuer `algo/validate.py`, Regressions-Signifikanztest fuer Strategie-Alpha) in der Synthese-Seite dokumentiert, ebenfalls noch nicht implementiert. |
| 2026-08-12 | **Ingest der restlichen 9 MIT-15.S08-Transkripte** (Lectures 12/14/18/19/20/21/23/24/25, `raw/quant-finance/2026/`) — Formeln zu Zeitreihenanalyse, formaler Brownscher Bewegung, Volatilitaetsmodellen (GARCH), Black-Scholes/risikoneutraler Bewertung, Machine-Learning-Grundlagen und Itô-Kalkül/SDEs ins Wiki uebernommen (5 neue Konzeptseiten + 9 Sourceseiten + Erweiterung von `wiki/concepts/Wahrscheinlichkeitstheorie & Stochastische Prozesse für Finance.md` + Erweiterung der Synthese-Seite). Lecture 18 (Biomedical AI) und Lecture 20 (Kalshi/CFTC) bewusst nur als schlanke Source-Seiten angelegt — kein uebertragbarer Formel-/Regel-Content. **Zwei neue, noch nicht getestete Thesen** (siehe Backlog-Abschnitt oben "zwei quant-finance-Thesen aus MIT-15.S08-Batch-2-Ingest"): (1) Box-Pierce-Autokorrelationstest auf MNQ-Returns ueber mehrere Timeframes, (2) GARCH(1,1)-basierte dynamische Stop-Distance/Sizing-Regel statt festem ATR-Multiplikator. Beide noch ohne Backtest-Skript, folgen in einer kommenden Session. Black-Scholes/risikoneutrale Bewertung explizit als "aktuell nicht direkt anwendbar" markiert (MNQ ist Future, kein Optionsprodukt); Machine-Learning-Train/Val/Test-Disziplin als Bestaetigung der bestehenden `algo/validate.py`-Methodik eingeordnet, kein neuer Code. |
| 2026-08-12 | **Ingest 23-teilige Yale-"Financial Markets"-Playlist** (Robert Shiller, Econ 252, Fruehjahr 2011, `raw/financial-markets-2011-with-robert-shiller/` — Playlist-Titel liess urspruenglich MIT vermuten, tatsaechliche Quelle ist Yale/YaleCourses, korrekt benannt). 23 Sourceseiten + 9 Konzeptseiten mit Schwerpunkt Risikomanagement (Portfoliotheorie/Diversifikation, Versicherung als Institution inkl. AIG-Fallstudie, VaR/CoVaR/Unabhaengigkeitsannahme, Efficient Markets, Behavioral Finance, Fisher-Zins/Leverage-Zyklen, Optionen, Regulierung, Forward-/Futures-Maerkte). Sechs Backlog-Punkte mit Algo-Bezug dokumentiert (siehe Abschnitt oben "Risikomanagement-Prinzipien aus Yale-Econ-252-Ingest") — hoechste Prioritaet: Korrelationsbruch-Check fuer `backtest_ensemble.py` (Strategien koennen im Stress synchron laufen statt diversifiziert) und Fat-Tail-Reporting-Ergaenzung fuer `validate.py`/`stress_test.py` (Bootstrap aus eigener Historie unterschaetzt Tail-Risiko strukturell). Gambler's-Ruin-Formel und Random-Walk-Argument bestaetigen nur Bestehendes, kein neuer Code. Noch keine Backtest-Skripte gebaut, folgt in kommender Session. |
| 2026-08-12 | **Quant-Riskmanagement: vier austauschbare Risk-Module gebaut** (Spec: `docs/superpowers/specs/2026-08-12-quant-risk-management-design.md`, Plan: `docs/superpowers/plans/2026-08-12-quant-risk-management.md`). Neu: `algo/risk_fixed.py` (Status quo, feste 1 %), `algo/risk_garch.py` (GARCH(1,1)-Vol-Skalierung von `base_pct`, MLE ueber `scipy.optimize`, kein neues Package, geclippt auf [0,5, 1,5] x `base_pct`), `algo/risk_kelly.py` (Half-Kelly `f* = p - (1-p)/b` ueber die letzten 30 abgeschlossenen Trades), `algo/risk_killswitch.py` (Drawdown-Gate VOR dem Sizing). Gemeinsames Interface `risk_pct(base_pct=0.01, **ctx) -> float`, in `SilverBulletStrategy.risk_module` verdrahtet und damit strategie-unabhaengig austauschbar; Trennung "wie viel % Risiko" (diese Module) von "wie viele Kontrakte bei diesem %" (unveraendert `pnl.py::risk_size()`). Neuer Vergleichs-Harness `algo/backtest_risk_compare.py` fuehrt alle drei Sizing-Module gegen **dieselben** Silver-Bullet-Signale und schreibt `wiki/synthesis/Risk-Management-Vergleich (laufend).md` (Equity, Max-DD, Win-Rate, Profit Factor, Expectancy, echte $-P&L, `dubious_pct`, 95%-Tages-VaR/ES). Ergebnis MNQ: alle drei bei n=6 Trades, Win Rate 0 %, echte $-P&L -11.002 (fixed/kelly, identisch weil Kelly unter 20 Trades auf `base_pct` zurueckfaellt) bzw. -10.385 (garch) — **kleine Stichprobe, Unterschiede nicht aussagekraeftig**, steht als Vorbehalt in der generierten Seite. |
| 2026-08-12 | **Kritischer Punktwert-Bug im Drawdown-Kill-Switch gefunden und gefixt (finale Whole-Branch-Review).** `SilverBulletStrategy.next()` fuetterte den Kill-Switch mit `self.equity` der `backtesting`-Lib — die ist in ROHEN Preispunkt-Einheiten denominiert (intern $1/Punkt), nicht in echten Dollar. **Exakt derselbe Fehlertyp wie der 2026-08-06-Audit-Fund in `backtest_ensemble.py::_risk_size`** und wie die bereits dokumentierte ⚠️-Grenze im Docstring von `pnl.py::risk_size()`. Folge: die 15%-Schwelle bedeutete nicht 15 % des echten Kontos. Fix: neues Klassenattribut `starting_cash = 100_000` (muss zum `Backtest(cash=...)` passen, in `main()` und `backtest_risk_compare.py::run_one()` gesetzt), Umrechnung `real_equity = starting_cash + (lib_equity - starting_cash) * point_value` vor dem Gate. **Zahlen-Auswirkung bei MNQ ($2/Punkt): der Switch trippt jetzt deutlich FRUEHER, nicht spaeter** — 22 Trades / -20,46 % Lib-DD vorher gegen **6 Trades / -8,72 % Lib-DD** jetzt, weil ein realer 15%-Drawdown bei doppeltem Punktwert schon bei ~7,5 % Lib-Drawdown erreicht ist. Regressionscheck: mit `max_drawdown_pct = 1.0` (Switch aus) reproduziert der Lauf die historische Baseline exakt (107 Trades, -32.032,82 USD, -38,45458 % Max-DD) — die Umrechnung ist per Konstruktion ein No-op, solange der Switch nicht ausloest, sie beruehrt nur den Vergleich, nicht die Sizing-/PnL-Arithmetik. **Weitere Funde derselben Review, alle gefixt:** (1) beim Trip wurden bereits platzierte, noch nicht gefuellte Limit-Orders aus frueheren Bars nicht storniert — die verfallen in diesem Setup nie (siehe `algo/README.md`) und konnten nach dem Stop noch fuellen, was die "keine neuen Trades"-Garantie aushebelte; jetzt `for o in self.orders: o.cancel()` im geblockten Zweig. (2) `risk_kelly.risk_pct()` war nach oben unbegrenzt (rohes Half-Kelly reicht mathematisch bis 0,5 = 50 % Kontorisiko pro Trade), waehrend `risk_garch` bewusst auf [0,5, 1,5] x `base_pct` clippt — jetzt dieselbe Obergrenze `1,5 x base_pct`; Nebenbefund: der bestehende `demo()`-Testfall (p=0,5/b=2 -> Half-Kelly 0,125) lag entgegen der Review-Annahme **ueber** der Kappung bei `base_pct=0,01`, der Test wurde entsprechend ehrlich umgebaut (ungekappter Fall jetzt mit `base_pct=0,1`, zwei explizite Kappungsfaelle dazu). (3) `risk_killswitch.allowed()` rechnete `max(equity_curve)` pro Bar ueber die wachsende Liste, bei ~11.500 Bars also O(n²) (gleiche Fehlerform wie der `extend_hist()`-Fix von 2026-08-11); Signatur jetzt `allowed(peak, current, max_drawdown_pct)` mit inkrementell mitgefuehrtem `SilverBulletStrategy._equity_peak`. (4) `run_one()` liess `SilverBulletStrategy.risk_module` mutiert stehen -> `try/finally` setzt jetzt auf `risk_fixed` zurueck; obskurer NaN-Selbstvergleich durch `pd.notna()` ersetzt; Spalte `Expectancy %` im Vergleichsreport ergaenzt (war in der Spec gefordert, fehlte in der Implementierung), kaputter Wikilink `[[../algo/README.md|...]]` auf ein Code-README durch Code-Span ersetzt. (5) Doku praezisiert: der Dauerstopp ist **beabsichtigt** (Nutzerentscheidung fuers Backtest-Stadium — bei flacher Position bewegt sich die Equity nicht, also kann kein neues Hoch und damit kein Auto-Reset entstehen; kein Timer/Decay-Reset eingebaut), und die Schwelle wird "erreicht", nicht "ueberschritten" (inklusiv). `selfcheck.py` 16/16 gruen. Bericht: `.superpowers/sdd/2026-08-12-quant-risk-management/final-fix-report.md`. |
| 2026-08-13 | **NDOG-/NWOG-Historie in `algo/live_status.py`** (Anlass: Daily-Bias-Journal 13.08. mit der bislang unbacktesteten Nutzer-These "NDOG bleibt mind. 5 Handelstage, NWOG mind. 5 Handelswochen als DOL aktiv", siehe `wiki/concepts/New Day Opening Gap (NDOG).md`). Neue reine Funktion `open_gap_history(daily_bars, upto_day, n, weekly)`: reused `tools/analyze_ohlc.py::ndog_gap()`/`nwog_gap()` fuer die letzten 5 Handelstage/-wochen vor `upto_day`, prueft aber zusaetzlich ueber alle Tage bis heute (nicht nur den Gap-Tag selbst), ob das Level je wieder beruehrt wurde. Zwei neue JSON-Felder `ndog_history`/`nwog_history` (nur unerreichte Level). Dazu `fetch_today()` um `1d_unfiltered` (~70 Kalendertage) erweitert; `_bars_from_df()` behandelt jetzt tz-naive 1d-Kerzen korrekt (yfinance liefert Daily ohne Zeitzone, vorher Crash bei `tz_convert`). `--dry-run` liefert die neuen Felder bewusst leer (kein Multi-Day-Fetch dort). Neuer Selftest mit synthetischen Tageskerzen. Live-Lauf 13.08. zeigt 2 offene NDOG (07.08. @ 29.488,25; 12.08. @ 29.626,00) und 1 offenes NWOG (03.08. @ 28.404,25), alle unter dem aktuellen Preis. Backtest der 5-Tage/5-Wochen-These selbst steht noch aus (offener Punkt). Alle 4 `live_status.py`-Selftests + alle 16 `selfcheck.py`-Checks gruen. |
| 2026-08-13 | **Root-Cause gefunden: ORG-Open 12.08. weicht von Jannes' Chart ab (29.991,75 CSV vs. 29.991,25 Chart, dazu 1p-FVG-Grenzen um 0,25 Pkt versetzt).** Systematisch nach `superpowers:systematic-debugging` geprueft: (1) Timestamp der 9:30-Kerze via Epoch->NY-Konvertierung verifiziert, korrekt. (2) `raw/marktdaten/2026/08/12.08.2026/MNQ 2026-08-12 1m RTH.csv` stammt laut `git log` aus dem Bulk-Commit `df5cb38de` (`push.ps1`, keine Einzelzuordnung moeglich) und enthaelt fuer diesen Tag **nur** `fetch_yfinance.py`-Output (kein manueller TradingView-Export vorhanden, anders als an Tagen mit `(2)`/`(3)`-Dateien). (3) Live-Re-Query von `yfinance.download("MNQ=F", interval="1m")` fuer denselben Zeitstempel reproduziert exakt dieselben Werte wie die CSV (Open 29.991,75) — **kein Parsing-/Pipeline-Bug**, die CSV gibt den Yahoo-Feed korrekt wieder. **Root Cause: Vendor-Diskrepanz** — Yahoos `MNQ=F`-Kontinuierlicher-Kontrakt-Feed weicht auf Tick-Ebene von Jannes' Chart-/Broker-Quelle (vermutlich TradingView/CME direkt) ab; bislang war in `CLAUDE.md` nur der **Zeit**versatz von yfinance dokumentiert, nicht ein **Preis**versatz. Kein Code-Fix noetig/moeglich (kein Bug), aber neue Datenqualitaets-Grenze: **fuer praezisionskritische Analysen (ORG-C.E., Q/O/H-Level, FVG-Grenzen) yfinance-only-Tage wie 12.08. gegen TradingView-Export gegenpruefen, bevor Level als exakt gelten** — Nachtrag noetig in `wiki/concepts/ORG (Opening Range Gap) & 1st Presented FVG.md` und ggf. `CLAUDE.md`. |
| 2026-08-13 | **Vendor-Diskrepanz vom selben Tag bestaetigt + Daten ersetzt.** Jannes' TradingView-Export (`raw/CME_MINI_MNQU2026, 1_51f11.csv`, lose Datei, noch nicht ins `raw/marktdaten/`-Schema einsortiert -- Original bewusst nicht geloescht) reproduziert exakt Open 29.991,25 und 1.p-FVG 29.929,25-29.964,75, bestaetigt damit die Root-Cause-Analyse von oben. **1380 von 1440 Minuten fuer den 12.08.**, einzige Luecke 16:59-18:00 NY (reguläre CME-Wartungspause, kein Artefakt). `raw/marktdaten/2026/08/12.08.2026/MNQ 2026-08-12 1m.csv` (vorher nur 210 Minuten, 00:00-03:29) und `... 1m RTH.csv` (391 Minuten, aber falsche Preise) durch die vollstaendigen/korrekten TradingView-Daten ersetzt. 11.08. **nicht** angefasst -- bestehende Datei dort bereits mit 1381 Minuten near-vollstaendig, die neue Export-Datei deckt den 11.08. nur ab 03:39 ab (schlechter). 13.08. (169 Minuten, 00:00-02:48) bewusst noch **nicht** als Tagesdatei angelegt -- Handelstag laeuft noch, folgt beim naechsten vollstaendigen Nachzug. |
| 2026-08-13 | **Marktdaten-Ingest: MNQ 1m 13.08. auf 1180 Kerzen erweitert, Higher-TFs neu gebaut.** Neuer TradingView-Export `CME_MINI_MNQU2026, 1_6f7e6.csv` (3478 Kerzen, 11.08. 01:42 – 13.08. 13:39 NY) via `algo/ingest_tvexport.py` eingespielt: 13.08. 825 → 1180 Kerzen (+355), **0 Lücken**, 1 von TradingView revidierte Kerze. Fehlende 200 Kerzen = 13:40–17:00 NY, Handelstag beim Export noch offen (kein Datenfehler). Higher-TFs mit `tools/resample_1m.py` aus der neuen 1m-Basis regeneriert (5m 236 / 15m 79 / 1h 20 / 4h 6 / 1d 1) — die alten waren aus nur 825 Kerzen gebaut und damit für alles ab ~09:00 NY falsch, insbesondere die 1d-Kerze (High/Low/Close des Tages). **Vorgehensregel bestätigt:** vor jedem Ingest Dry-Run (`ingest(..., schreiben=False)`) über *alle* losen Exporte laufen lassen. Die 6 älteren Exporte (11./12.08. MNQ/ES/YM/DXY) brachten 0 neue Kerzen, hätten aber wegen der Konfliktregel „neuer Export gewinnt" ältere Exportstände über den Bestand geschrieben (`..._33e80.csv` allein 22 abweichende Kerzen am 10.08.). Nicht eingespielt. **Nebenbefund:** die am 13.08. im Daily-Bias vermerkte Lücke „12.08. nur 18:00–03:29 NY" ist geschlossen, 12.08. steht auf 1380/1380 ohne Lücke. |
| 2026-08-13 | **Marktdaten-Nachzug + neues Werkzeug `algo/backfill_yfinance.py`.** (1) **Strukturlücke gefunden:** `fetch_yfinance.write_day()` überschreibt grundsätzlich nie — richtig zum Schutz der TradingView-Daten, aber dadurch friert ein *abgebrochener* Abruf permanent ein, der nächste Lauf meldet nur "existiert bereits, übersprungen". Gemessen: alle 10 Forex-Paare standen für den 11.08. seit dem abgebrochenen Lauf vom 11.08. bei ~765 statt ~1440 Kerzen (Ende 06:45 NY), MNQ 03.08. bei 300 statt 1369. Die Spec `docs/superpowers/specs/2026-08-12-marktdaten-schicht-design.md` benennt den Fall ("Abgebrochener Download"), hatte aber kein Werkzeug dagegen. **Fix:** `backfill_yfinance.py` ergänzt nur fehlende Zeitstempel in *bestehenden* Dateien, Konfliktregel umgekehrt zu `ingest_tvexport.py` (**Bestand gewinnt**, abweichende Kerzen werden gezählt und berichtet, nie überschrieben) — damit gilt Spec 3.2 "yfinance überschreibt nie" weiter wörtlich. Reuse statt Neubau: `download_interval`/`trading_day` aus `fetch_yfinance.py`, `lies`/`schreib`/`luecken` aus `ingest_tvexport.py`; `demo()`-Selbstcheck enthalten. **Repariert:** 10 Forex-Paare 11.08. (~765→~1439), MNQ 03.08. (300→1369, dabei 52 abweichende Kerzen zwischen altem und neuem Yahoo-Abruf — Bestand behalten, Abweichung hier notiert), MNQ 05.08. (1006→1365), MNQ 07.08. (617→1361), ES 05.08. (894→1366). (2) **Tiefhistorie 1d erschlossen** (yfinance unbegrenzt zurück, bisher ungenutzt): ES/NQ je 6539 Tage ab 2000, YM 6130 ab 2002, RTY 2289 ab 2017, MNQ 154→1833 ab 2019 — verbreitert die Basis von `stress_test.py` erheblich. Stichprobe ES 2008-10-10 (913,5/943,0/837,0/891,0) plausibel. (3) **Neu geschlossen:** Forex 12.08. (10 Paare, fehlte ganz), ES 06./07.08. (fehlten ganz), höhere TFs für ES 10./11.08. aus 1m gerechnet. (4) ⚠️ **Befund AUDUSD/NZDUSD:** yfinance liefert für diese beiden Paare über `interval="1m"` durchgängig nur jede zweite Minute (719/1440 Kerzen, 717×120 s Abstand, kein einziger 60-s-Abstand; Gegenprobe EURUSD 1437×60 s). Paar-spezifische Quellgrenze, durch erneuten Abruf nicht behebbar. Jede minutengenaue Auswertung dieser Paare lief und läuft auf halber Auflösung — dokumentiert in `algo/README.md`, gehört in den Torwächter (Spec 3.1). (5) **Offene Inkonsistenz:** die aus dem *alten, kürzeren* 1m gerechneten 5m/15m/1h/1d-Dateien der reparierten Tage sind jetzt veraltet. Bewusst nicht überschrieben (Spec-Entscheidung 5: nicht löschen, der Loader soll den aus 1m gerechneten Wert vorziehen) — `backtest_common.load_range()` ist noch nicht gebaut, bis dahin bleibt das offen. (6) Nicht per yfinance reparierbar, weil es fremde Vendor-Preise in TradingView-Dateien mischen würde: ES 12.08. 573/1380, YM 10./11./12.08., DXY 10./11./12.08. — brauchen neue TradingView-Exporte. `selfcheck.py` 16/16 grün. |
| 2026-08-13 | **FVG-Grenzen-Bug gefunden und gefixt (Nutzer-Korrektur an der 12-13-NY-Auswertung).** Jannes stellte 14 TradingView-Screenshots (`Dropbox/TradingJournal/Screenshots/13-08-2026`) mit selbst eingezeichneten MNQ-FVG-Boxen als Referenz bereit — teils mit, teils ohne VII. Systematisch nach `superpowers:systematic-debugging` gegengerechnet (alle Boxen liegen im Zeitraum 13.08. 00:01–12:53 und damit vollstaendig in `raw/marktdaten/2026/08/13.08.2026/MNQ 2026-08-13 1m.csv`, B-ADJ-Chart und CSV stimmen 1:1 ueberein). **Root Cause in `tools/analyze_ohlc.py::fvgs()`**: die VII-Kanten waren als feste OHLC-Felder verdrahtet (`lo = a.c if m.o > a.c else a.h` usw.). Das ist nur korrekt, solange alle drei Kerzen in Richtung des Moves schliessen — bei einer **Gegenkerze** (bearishe Kerze 1 bzw. 3 in einem bullishen FVG) sind Open und Close vertauscht, die Grenze landet mitten im Koerper und das VII-Kriterium meldet eine Luecke, die der Koerper selbst schon gehandelt hat. **Fix:** Koerperkante durchgaengig ueber `max(o,c)`/`min(o,c)`; dieselbe Korrektur in `viis()` (dort war `b.o > a.c` das Kriterium statt Koerper gegen Koerper). **Verifikation: 13/13 Referenzboxen exakt getroffen** (vorher 11/13). **Zahlen-Auswirkung 13.08. 12:00–13:00 NY:** 1m-FVG 12:26 war 30.165,25–30.184,50 (19,25p, C.E. 30.175,00), korrekt ist 30.165,25–**30.180,00** (14,75p, C.E. 30.172,75) — Kerze 12:26 ist bearish, ihre Koerper-Unterkante (Close 30.180,50) liegt unter der Oberkante von Kerze 12:25 (30.184,00), also keine VII, Grenze bleibt der Wick. 5m-FVG 12:05 war 30.115,25–30.150,25 (35p, "beruehrt"), korrekt ist 30.137,25–30.150,25 (13p, **gefuellt 12:15**) — Kerze 11:55 ist bearish, die vermeintliche 0,25er-VII lag komplett innerhalb ihres Koerpers. Uebrige 11 Boxen unveraendert. Betrifft rueckwirkend alle Aufrufer (`rules.py::plan_trade`, `live_status.py`, `macro_db.py`, `mor_levels.py`, 5 Backtest-Skripte) — nicht neu gerechnet, aeltere FVG-Zahlen im Vault koennen an Gegenkerzen-Faellen abweichen. Regressionstest `tools/test_fvg_vii.py` um drei Faelle erweitert (zwei davon direkt aus Jannes' Boxen), `selfcheck.py` 16/16 gruen. Wiki-Praezisierung auf `wiki/concepts/Fair Value Gap (FVG).md` + `wiki/concepts/Volume Imbalance (VII).md`: "Close gegen Open" ist eine Kurzform, gemeint ist Koerperkante gegen Koerperkante. |
| 2026-08-13 | **Zweiter FVG-Fehler gefunden (Zeitstempel) + neue Einstufung stark/normal.** Jannes meldete, die FVG seien weiterhin "nicht richtig eingezeichnet". Systematisch geprueft: (1) Boundaries erneut gegen alle 13 Referenzboxen — 13/13 exakt, (2) alle 14 FVG des 11-12-Uhr-Laufs per Hand gegen die Roh-OHLC nachgerechnet — alle regelkonform, (3) Hypothese "seine Boxen sind der strukturbrechende Subset" per Backtest **verworfen** (12:14 gezeichnet ohne Break, 12:01 nicht gezeichnet mit Break — die Boxen waren Stichproben), (4) 5m-Resampling gegen 1m-Aggregat geprueft — deckungsgleich, keine TF-Drift. **Root Cause: kein Detektor-Bug, sondern ein Reporting-Bug von mir.** `fvgs()` liefert korrekt `t` = mittlere (Displacement-)Kerze; in meinen Auswertungsskripten hatte ich auf die *dritte* Kerze umgerechnet. Folge: jede Box sitzt beim Gegenpruefen im Chart eine Kerze zu weit rechts (sein Fib-Anker 12:24-12:26 heisst 12:25, bei mir stand 12:26), und am Fensterrand verschwindet ein FVG ganz — das 12:59er (30.188,50-30.194,00, **stark, offen**) fehlte in der 12-13-Liste, weil seine dritte Kerze auf 13:00 faellt. **Fix:** `fvgs()` gibt zusaetzlich `t_start`/`t_end` (Drei-Kerzen-Spanne) aus, Auswertungen filtern und labeln ueber die Displacement-Kerze. **Neue Nutzerregel implementiert** ("ein valides starkes FVG muss einen Swing High/Low brechen, was einen MSS/BOS hervorbringt; in Zukunft geht es immer um High Probability"): neue Funktion `analyze_ohlc._grade()`, laeuft automatisch am Ende von `fvgs()`, liefert `strong`/`broke` (`close`/`wick`/None)/`swing`/`ms`. Zwei Fallstricke bewusst abgedeckt: kein Lookahead (Swing muss bei Kerze 1 bestaetigt sein) und **nur intakte Swings** (ein bereits per Close genommenes Level faellt raus — erste Implementierung stufte sonst 11:34 als "stark" gegen den Swing 30.202,25 ein, den 11:28 schon gebrochen hatte). Ergebnis 13.08.: von 14 1m-FVG in 11-12 sind **4 stark** (11:10, 11:28 MSS, 11:34, 11:51 BOS), von 13 in 12-13 sind **4 stark** (12:00, 12:01, 12:25 BOS, 12:59). Regressionstest `tools/test_fvg_vii.py` um zwei Faelle erweitert (Zeitstempel = Displacement-Kerze, Swing-Break-Einstufung), 13/13 Referenzboxen weiterhin exakt, `selfcheck.py` 16/16 gruen. |

### [2026-08-13] Datenqualitaet | MNQ-Tiefhistorie 1d: zwei defekte Sammeldateien aussortiert

**Anlass:** Beim Suchen der 05.08.-Premium-Wick fielen in `MNQ 2026-07-31 1d.csv` und
`MNQ 2026-08-03 1d.csv` (je ~290 Daily-Bars, aus dem Tiefhistorie-Lauf vom 31.07./03.08.) je
**71 degenerierte Bars** auf: `open == high` und `low == close`, Zeitraum 2025-06-09 .. 2025-12-18.
Eine Wick-Analyse auf diesen Tagen ergibt rechnerisch 0 Punkte Wick-Range.

**Untersuchung (drei Runden, zwei davon widerlegten die jeweils vorige Hypothese):**

| Hypothese | Test | Ergebnis |
|---|---|---|
| "nur 71 Bars kaputt" | alle 230 Ueberlappungstage gegen frischen Abruf | **falsch** — alle 230 weichen ab |
| "yfinance liefert generell Muell" | frischer Abruf gegen TradingView-verifizierte Einzeltage 04.–13.08. | **falsch** — O/H/L exakt gleich |
| "alte Werte richtig, nur falsch datiert" | O/H/L/C-Tupel im frischen Abruf suchen | schien widerlegt (0 Treffer) … |
| … Korrektur: Test war zu streng | nur O/H/L vergleichen (Close = Settlement vs. Last) | **29 Treffer, davon 28 mit Versatz +1 Tag** |

Die Sammeldateien sind also mehrfach defekt: 71 degenerierte Bars, **Datums-Offset +1 Tag** im
2026er Teil, und im 2025er Teil preislich gar nicht reproduzierbar (~1 300–1 500 Punkte Versatz).
Der Datums-Offset ist genau der Fehlertyp, vor dem "Zeit vor Preis" in CLAUDE.md warnt.

**Entscheidender Befund — der aktive Bestand war nie betroffen:** Die primaere Datenebene sind die
Ein-Tages-Dateien, nicht die Sammeldateien. Vollpruefung aller **1 833 MNQ-1d-Tagesdateien**
(2019-05 bis 2026-08) gegen einen frischen `MNQ=F`-Abruf: **0 abweichend, 0 degeneriert.**
Der Defekt lag in einem redundanten Parallel-Strang.

**Massnahmen:**
- Beide Dateien nach `raw/marktdaten/_defekt/tiefhistorie-1d-2026-08/` verschoben (nicht geloescht),
  mit README, das Befund und Beweisfuehrung festhaelt.
- `python algo/fetch_yfinance.py 2025-06-06 2026-08-13 --intervals 1d` nachgezogen: 2 Dateien neu
  (31.07. und 03.08.2026 — deren Dateinamen hatten die Sammeldateien belegt), alle uebrigen
  existierten bereits korrekt.
- Lueckenpruefung 2025-06-06 .. 2026-08-13: 406 Handelstage vorhanden, 11 fehlende Wochentage —
  alle 11 sind US-Feiertage (Juneteenth, Labor Day, Thanksgiving, Christmas, New Year, MLK,
  Presidents Day, Good Friday, Memorial Day, Independence Day observed). **Keine echte Luecke.**
- Aktiver Bestand enthaelt jetzt **0** Sammel-1d-Dateien; das Schema ist wieder durchgaengig
  "eine Datei pro Handelstag".

**Nachtrag selben Tags — Guard gebaut (Nutzerfreigabe: "wir muessen IMMER sicher gehen, dass die
Daten korrekt sind, zur Not doppelt gegenpruefen").** `analyze_ohlc.pruefe_kerzen()` als
Nulltoleranz-Gate vor **allen sechs** Schreibpfaden nach `raw/marktdaten/` (`fetch_yfinance`,
`ingest_tvexport` inkl. `backfill_yfinance`, `resample_1m`, `ingest_histdata_xlsx`,
`fetch_dukascopy`) — eine Funktion an der geteilten Basis statt sechs Einzelguards. Details in
`README.md`, Abschnitt "OHLC-Nulltoleranz-Gate".

Zweite Ebene fuer das, was ein lokaler Check prinzipiell nicht sehen kann:
`pruefe_gegen_referenz()` vergleicht gegen eine unabhaengige Quelle — **nur O/H/L, nie den
Close**. Genau dieser Fehlschluss passierte heute: der Close weicht zwischen Feeds systematisch ab
(Settlement vs. letzter Trade), ein Vergleich inklusive Close meldete faelschlich "0 Treffer" und
liess die Daten wie voellig andere Preise aussehen, obwohl O/H/L identisch und nur um einen Tag
verschoben waren.

Verifiziert gegen den Realfall: die aussortierte Datei wird abgelehnt (21 % degeneriert), eine
gesunde Datei aus dem Bestand geht durch, eine einzelne degenerierte 1m-Kerze loest keinen
Fehlalarm aus, und ein E2E-Lauf ueber `write_day` legt die defekte Datei gar nicht erst an.
Regressionstest `ohlc_gate` in `selfcheck.py`, jetzt 18 Selbstchecks.

**Weiter offen:** Ursache des Defekts im Tiefhistorie-Lauf selbst ist nicht geklaert (Yahoo-Antwort
zum damaligen Abrufzeitpunkt nicht rekonstruierbar). Der Guard verhindert die Wiederholung, erklaert
sie aber nicht. Ebenfalls offen: `pruefe_gegen_referenz` ist gebaut, aber noch in keinen
Abruf-Workflow als Pflichtschritt eingehaengt — bisher manuell aufzurufen.
| 2026-08-13 | **Vier neue FVG-Staerke-Thesen geloggt und gebacktestet** (neues Skript `algo/backtest_fvg_strength.py`, 27 MNQ-1m-Handelstage, 7.279 FVG, 6.851 simulierte Trades; Regel: Limit am C.E., Stop ferne Kante, Ziel 2R, echter Punktwert $2, Breakeven 33,3 %). Thesen: (T1) FVG-Groesse haengt an Session/Vola, 9:30-Open >> London, NY PM zurueck Richtung London; (T2) starkes FVG bricht Swing/MSS; (T3) gross + MSS = High Probability; (T4) Ueberlappung mit Higher-TF-PD-Array-Qs bzw. NDOG/NWOG hebt die Wahrscheinlichkeit. **T1 klar bestaetigt:** NY Open Median-FVG 13,50 Pkt / Median-1m-Kerze 29,38 gegen London 4,75 / 10,12 — Faktor 2,8 bzw. 2,9; NY PM 5,75 / 13,00, also zurueck Richtung London, aber 20-28 % darueber (Gleichsetzung leicht untertrieben). **Kernbefund:** das Verhaeltnis FVG-Groesse ÷ lokale Kerzenrange liegt in *jeder* Session bei 0,44-0,48 (nur NY AM 0,32) — ein FVG ist immer rund die halbe Kerzenrange gross, es aendert sich nur der Massstab. Folge: Groessen-Schwellwerte gehoeren auf `size_rel = size / Median-Range(30 Kerzen)`, nie auf feste Punkte. **T2/T3 mit hartem Vorbehalt:** roh sehen grosse FVG stark besser aus (Win 45,6 % / 4,25 $ gegen 31,5 % / 1,26 $ bei normalen), aber `dubious_pct` (Stop UND Ziel in derselben 1m-Kerze, konservativ als Verlust gewertet) ist bei kleinen FVG **55,9 %** gegen 27,3 % bei grossen. Rechnet man die strittigen Faelle heraus, **dreht die Rangfolge** (71,5 % gegen 62,8 %) — auf Minutendaten ist der Groesseneffekt **nicht entscheidbar**. Sauber vergleichbar sind nur Gruppen mit aehnlicher dubious-Quote: `nur gross` (27,3 %) gegen `stark + gross` (26,5 %) ergibt 45,6 → 45,8 % bzw. 62,8 → 62,3 % — **der Swing-Break bringt bei kontrollierter Groesse nichts Messbares, T2 ist in dieser Form nicht belegt**, die Groesse erklaert den Effekt. Belastbar bleibt: kleine FVG sind auf 1m nicht sinnvoll *ausfuehrbar* (Stop und Ziel in einer Kerze), das ist eine Ausfuehrbarkeits-, keine Wahrscheinlichkeitsfrage. **T4 gespalten:** `stark + gross + HTF-Qs` 47,9 % / **6,33 $ je Trade** (bester Wert der Tabelle) gegen `stark + gross` 45,8 % / 4,63 $ bei nahezu gleicher dubious-Quote — haelt als kleiner, konsistenter Zusatzeffekt. `stark + gross + NDOG` liegt mit 44,4 % / 55,2 % auf **beiden** Metriken darunter — **kein Beleg fuer die NDOG-Confluence**, n=144 aber auch kein Gegenbeweis. Aufloesen laesst sich der Messvorbehalt erst mit Ausfuehrungsdaten feiner als 1m (Sekunden/Ticks, kommt mit IBKR, Roadmap-Punkt 4). Reuse: `find_days()` in `backtest_common.py` aus `find_1d_days()` generalisiert. Ergebnisseite `wiki/synthesis/FVG-Stärke, Session-Volatilität & Confluence (laufend).md`, Rohwerte `algo/results/fvg_strength.json`. `selfcheck.py` jetzt 18/18 (neuer Selbstcheck fuer die Trade-Simulation inkl. Stop-und-Ziel-in-einer-Kerze). |
